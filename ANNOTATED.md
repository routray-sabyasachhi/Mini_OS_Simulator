# ANNOTATED.md

Line-by-line explanations for every file in the project.

## File: cpu_scheduling\cpu_utils.h
- L1: `#ifndef CPU_UTILS_H` — Start include guard to prevent double inclusion.
- L2: `#define CPU_UTILS_H` — Define include guard macro.
- L3: `` — Blank line for readability and separation.
- L4: `#include <algorithm>` — Include required standard or project header so symbols compile.
- L5: `#include <iomanip>` — Include required standard or project header so symbols compile.
- L6: `#include <iostream>` — Include required standard or project header so symbols compile.
- L7: `#include <map>` — Include required standard or project header so symbols compile.
- L8: `#include <numeric>` — Include required standard or project header so symbols compile.
- L9: `#include <sstream>` — Include required standard or project header so symbols compile.
- L10: `#include <string>` — Include required standard or project header so symbols compile.
- L11: `#include <vector>` — Include required standard or project header so symbols compile.
- L12: `` — Blank line for readability and separation.
- L13: `struct Process {` — Define a record type to group related fields.
- L14: `    int pid;` — Execute this statement as part of the algorithm or UI flow.
- L15: `    int arrival;` — Execute this statement as part of the algorithm or UI flow.
- L16: `    int burst;` — Execute this statement as part of the algorithm or UI flow.
- L17: `    int priority;` — Execute this statement as part of the algorithm or UI flow.
- L18: `};` — End a scope block.
- L19: `` — Blank line for readability and separation.
- L20: `struct GanttEntry {` — Define a record type to group related fields.
- L21: `    int pid; // -1 for idle` — Execute this statement as part of the algorithm or UI flow.
- L22: `    int start;` — Execute this statement as part of the algorithm or UI flow.
- L23: `    int end;` — Execute this statement as part of the algorithm or UI flow.
- L24: `};` — End a scope block.
- L25: `` — Blank line for readability and separation.
- L26: `struct ScheduleMetrics {` — Define a record type to group related fields.
- L27: `    double avg_waiting;` — Execute this statement as part of the algorithm or UI flow.
- L28: `    double avg_turnaround;` — Execute this statement as part of the algorithm or UI flow.
- L29: `    double avg_response;` — Execute this statement as part of the algorithm or UI flow.
- L30: `};` — End a scope block.
- L31: `` — Blank line for readability and separation.
- L32: `struct ScheduleResult {` — Define a record type to group related fields.
- L33: `    std::vector<Process> processes;` — Execute this statement as part of the algorithm or UI flow.
- L34: `    std::vector<int> completion;` — Execute this statement as part of the algorithm or UI flow.
- L35: `    std::vector<int> waiting;` — Execute this statement as part of the algorithm or UI flow.
- L36: `    std::vector<int> turnaround;` — Execute this statement as part of the algorithm or UI flow.
- L37: `    std::vector<int> response;` — Execute this statement as part of the algorithm or UI flow.
- L38: `    std::vector<GanttEntry> gantt;` — Execute this statement as part of the algorithm or UI flow.
- L39: `    ScheduleMetrics metrics;` — Execute this statement as part of the algorithm or UI flow.
- L40: `};` — End a scope block.
- L41: `` — Blank line for readability and separation.
- L42: `` — Blank line for readability and separation.
- L43: `inline int sumVector(const std::vector<int>& v) {` — Inline utility for small, frequently-used logic.
- L44: `    return std::accumulate(v.begin(), v.end(), 0);` — Return from the function.
- L45: `}` — Close the current scope block.
- L46: `` — Blank line for readability and separation.
- L47: `inline ScheduleMetrics computeAverages(const std::vector<int>& waiting,` — Inline utility for small, frequently-used logic.
- L48: `                                      const std::vector<int>& turnaround,` — Execute this statement as part of the algorithm or UI flow.
- L49: `                                      const std::vector<int>& response) {` — Execute this statement as part of the algorithm or UI flow.
- L50: `    ScheduleMetrics m{};` — Execute this statement as part of the algorithm or UI flow.
- L51: `    if (!waiting.empty()) {` — Conditional check to choose a branch.
- L52: `        m.avg_waiting = static_cast<double>(sumVector(waiting)) / waiting.size();` — Execute this statement as part of the algorithm or UI flow.
- L53: `        m.avg_turnaround = static_cast<double>(sumVector(turnaround)) / turnaround.size();` — Execute this statement as part of the algorithm or UI flow.
- L54: `        m.avg_response = static_cast<double>(sumVector(response)) / response.size();` — Execute this statement as part of the algorithm or UI flow.
- L55: `    }` — Close the current scope block.
- L56: `    return m;` — Return from the function.
- L57: `}` — Close the current scope block.
- L58: `` — Blank line for readability and separation.
- L59: `inline void printProcessTable(const std::vector<Process>& procs,` — Inline utility for small, frequently-used logic.
- L60: `                              const std::vector<int>& completion,` — Execute this statement as part of the algorithm or UI flow.
- L61: `                              const std::vector<int>& waiting,` — Execute this statement as part of the algorithm or UI flow.
- L62: `                              const std::vector<int>& turnaround,` — Execute this statement as part of the algorithm or UI flow.
- L63: `                              const std::vector<int>& response) {` — Execute this statement as part of the algorithm or UI flow.
- L64: `    std::cout << "\n";` — Print formatted output to the console.
- L65: `    std::cout << std::left << std::setw(6) << "PID"` — Print formatted output to the console.
- L66: `              << std::setw(10) << "ARR"` — Execute this statement as part of the algorithm or UI flow.
- L67: `              << std::setw(10) << "BURST"` — Execute this statement as part of the algorithm or UI flow.
- L68: `              << std::setw(10) << "PRIO"` — Execute this statement as part of the algorithm or UI flow.
- L69: `              << std::setw(12) << "COMP"` — Execute this statement as part of the algorithm or UI flow.
- L70: `              << std::setw(12) << "WAIT"` — Execute this statement as part of the algorithm or UI flow.
- L71: `              << std::setw(12) << "TAT"` — Execute this statement as part of the algorithm or UI flow.
- L72: `              << std::setw(12) << "RESP" << "\n";` — Execute this statement as part of the algorithm or UI flow.
- L73: `    std::cout << std::string(84, '-') << "\n";` — Print formatted output to the console.
- L74: `    for (size_t i = 0; i < procs.size(); ++i) {` — Loop over a range of values.
- L75: `        std::cout << std::left << std::setw(6) << procs[i].pid` — Print formatted output to the console.
- L76: `                  << std::setw(10) << procs[i].arrival` — Execute this statement as part of the algorithm or UI flow.
- L77: `                  << std::setw(10) << procs[i].burst` — Execute this statement as part of the algorithm or UI flow.
- L78: `                  << std::setw(10) << procs[i].priority` — Execute this statement as part of the algorithm or UI flow.
- L79: `                  << std::setw(12) << completion[i]` — Execute this statement as part of the algorithm or UI flow.
- L80: `                  << std::setw(12) << waiting[i]` — Execute this statement as part of the algorithm or UI flow.
- L81: `                  << std::setw(12) << turnaround[i]` — Execute this statement as part of the algorithm or UI flow.
- L82: `                  << std::setw(12) << response[i] << "\n";` — Execute this statement as part of the algorithm or UI flow.
- L83: `    }` — Close the current scope block.
- L84: `}` — Close the current scope block.
- L85: `` — Blank line for readability and separation.
- L86: `inline void printMetrics(const ScheduleMetrics& m) {` — Inline utility for small, frequently-used logic.
- L87: `    std::cout << "\nAverages: "` — Print formatted output to the console.
- L88: `              << "Waiting=" << std::fixed << std::setprecision(2) << m.avg_waiting` — Execute this statement as part of the algorithm or UI flow.
- L89: `              << ", Turnaround=" << m.avg_turnaround` — Execute this statement as part of the algorithm or UI flow.
- L90: `              << ", Response=" << m.avg_response << "\n";` — Execute this statement as part of the algorithm or UI flow.
- L91: `}` — Close the current scope block.
- L92: `` — Blank line for readability and separation.
- L93: `#endif` — End include guard.

## File: cpu_scheduling\fcfs.cpp
- L1: `#include <algorithm>` — Include required standard or project header so symbols compile.
- L2: `#include <iostream>` — Include required standard or project header so symbols compile.
- L3: `#include <vector>` — Include required standard or project header so symbols compile.
- L4: `` — Blank line for readability and separation.
- L5: `#include "cpu_utils.h"` — Include required standard or project header so symbols compile.
- L6: `` — Blank line for readability and separation.
- L7: `namespace ui {` — Begin namespace scope for UI helpers.
- L8: `    int readInt(const std::string& prompt, int minValue, int maxValue);` — Execute this statement as part of the algorithm or UI flow.
- L9: `    void printSection(const std::string& title);` — Define a function that performs actions without returning a value.
- L10: `}` — Close the current scope block.
- L11: `` — Blank line for readability and separation.
- L12: `ScheduleResult runFCFS() {` — Execute this statement as part of the algorithm or UI flow.
- L13: `    ui::printSection("FCFS Scheduling");` — Execute this statement as part of the algorithm or UI flow.
- L14: `    int n = ui::readInt("Enter number of processes (1-10): ", 1, 10);` — Execute this statement as part of the algorithm or UI flow.
- L15: `` — Blank line for readability and separation.
- L16: `    std::vector<Process> procs(n);` — Execute this statement as part of the algorithm or UI flow.
- L17: `    for (int i = 0; i < n; ++i) {` — Loop over a range of values.
- L18: `        procs[i].pid = i + 1;` — Execute this statement as part of the algorithm or UI flow.
- L19: `        procs[i].arrival = ui::readInt("Arrival time P" + std::to_string(i + 1) + " (0-50): ", 0, 50);` — Execute this statement as part of the algorithm or UI flow.
- L20: `        procs[i].burst = ui::readInt("Burst time P" + std::to_string(i + 1) + " (1-50): ", 1, 50);` — Execute this statement as part of the algorithm or UI flow.
- L21: `        procs[i].priority = 0;` — Execute this statement as part of the algorithm or UI flow.
- L22: `    }` — Close the current scope block.
- L23: `` — Blank line for readability and separation.
- L24: `    std::vector<int> idx(n);` — Execute this statement as part of the algorithm or UI flow.
- L25: `    for (int i = 0; i < n; ++i) idx[i] = i;` — Loop over a range of values.
- L26: `    std::sort(idx.begin(), idx.end(), [&](int a, int b) {` — Sort indices by arrival to implement FCFS order.
- L27: `        if (procs[a].arrival == procs[b].arrival) return procs[a].pid < procs[b].pid;` — Conditional check to choose a branch.
- L28: `        return procs[a].arrival < procs[b].arrival;` — Return from the function.
- L29: `    });` — End a scope block.
- L30: `` — Blank line for readability and separation.
- L31: `    std::vector<int> completion(n, 0), waiting(n, 0), turnaround(n, 0), response(n, 0);` — Execute this statement as part of the algorithm or UI flow.
- L32: `    std::vector<GanttEntry> gantt;` — Execute this statement as part of the algorithm or UI flow.
- L33: `` — Blank line for readability and separation.
- L34: `    int time = 0;` — Execute this statement as part of the algorithm or UI flow.
- L35: `    for (int k = 0; k < n; ++k) {` — Loop over a range of values.
- L36: `        int i = idx[k];` — Execute this statement as part of the algorithm or UI flow.
- L37: `        if (time < procs[i].arrival) {` — Conditional check to choose a branch.
- L38: `            gantt.push_back({-1, time, procs[i].arrival});` — Execute this statement as part of the algorithm or UI flow.
- L39: `            time = procs[i].arrival;` — Execute this statement as part of the algorithm or UI flow.
- L40: `        }` — Close the current scope block.
- L41: `        int start = time;` — Execute this statement as part of the algorithm or UI flow.
- L42: `        response[i] = start - procs[i].arrival;` — Compute response time as first start minus arrival.
- L43: `        time += procs[i].burst;` — Execute this statement as part of the algorithm or UI flow.
- L44: `        completion[i] = time;` — Execute this statement as part of the algorithm or UI flow.
- L45: `        turnaround[i] = completion[i] - procs[i].arrival;` — Turnaround time = completion - arrival.
- L46: `        waiting[i] = turnaround[i] - procs[i].burst;` — Turnaround time = completion - arrival.
- L47: `        gantt.push_back({procs[i].pid, start, time});` — Execute this statement as part of the algorithm or UI flow.
- L48: `` — Blank line for readability and separation.
- L49: `        std::cout << "Step: P" << procs[i].pid << " runs from " << start << " to " << time << "\n";` — Print formatted output to the console.
- L50: `    }` — Close the current scope block.
- L51: `` — Blank line for readability and separation.
- L52: `    ScheduleResult result{procs, completion, waiting, turnaround, response, gantt, computeAverages(waiting, turnaround, response)};` — Execute this statement as part of the algorithm or UI flow.
- L53: `    return result;` — Return from the function.
- L54: `}` — Close the current scope block.

## File: cpu_scheduling\priority.cpp
- L1: `#include <algorithm>` — Include required standard or project header so symbols compile.
- L2: `#include <iostream>` — Include required standard or project header so symbols compile.
- L3: `#include <vector>` — Include required standard or project header so symbols compile.
- L4: `` — Blank line for readability and separation.
- L5: `#include "cpu_utils.h"` — Include required standard or project header so symbols compile.
- L6: `` — Blank line for readability and separation.
- L7: `namespace ui {` — Begin namespace scope for UI helpers.
- L8: `    int readInt(const std::string& prompt, int minValue, int maxValue);` — Execute this statement as part of the algorithm or UI flow.
- L9: `    void printSection(const std::string& title);` — Define a function that performs actions without returning a value.
- L10: `}` — Close the current scope block.
- L11: `` — Blank line for readability and separation.
- L12: `ScheduleResult runPriorityScheduling() {` — Execute this statement as part of the algorithm or UI flow.
- L13: `    ui::printSection("Priority Scheduling (Non-Preemptive)");` — Execute this statement as part of the algorithm or UI flow.
- L14: `    int n = ui::readInt("Enter number of processes (1-10): ", 1, 10);` — Execute this statement as part of the algorithm or UI flow.
- L15: `` — Blank line for readability and separation.
- L16: `    std::vector<Process> procs(n);` — Execute this statement as part of the algorithm or UI flow.
- L17: `    for (int i = 0; i < n; ++i) {` — Loop over a range of values.
- L18: `        procs[i].pid = i + 1;` — Execute this statement as part of the algorithm or UI flow.
- L19: `        procs[i].arrival = ui::readInt("Arrival time P" + std::to_string(i + 1) + " (0-50): ", 0, 50);` — Execute this statement as part of the algorithm or UI flow.
- L20: `        procs[i].burst = ui::readInt("Burst time P" + std::to_string(i + 1) + " (1-50): ", 1, 50);` — Execute this statement as part of the algorithm or UI flow.
- L21: `        procs[i].priority = ui::readInt("Priority P" + std::to_string(i + 1) + " (1-10, lower is higher): ", 1, 10);` — Execute this statement as part of the algorithm or UI flow.
- L22: `    }` — Close the current scope block.
- L23: `` — Blank line for readability and separation.
- L24: `    std::vector<int> completion(n, 0), waiting(n, 0), turnaround(n, 0), response(n, 0);` — Execute this statement as part of the algorithm or UI flow.
- L25: `    std::vector<GanttEntry> gantt;` — Execute this statement as part of the algorithm or UI flow.
- L26: `` — Blank line for readability and separation.
- L27: `    int time = 0;` — Execute this statement as part of the algorithm or UI flow.
- L28: `    int completed = 0;` — Execute this statement as part of the algorithm or UI flow.
- L29: `    std::vector<bool> done(n, false);` — Execute this statement as part of the algorithm or UI flow.
- L30: `` — Blank line for readability and separation.
- L31: `    while (completed < n) {` — Loop while a condition remains true.
- L32: `        int chosen = -1;` — Execute this statement as part of the algorithm or UI flow.
- L33: `        int bestPriority = 1e9;` — Track best (lowest numeric) priority among ready processes.
- L34: `        for (int i = 0; i < n; ++i) {` — Loop over a range of values.
- L35: `            if (!done[i] && procs[i].arrival <= time) {` — Conditional check to choose a branch.
- L36: `                if (procs[i].priority < bestPriority ||` — Track best (lowest numeric) priority among ready processes.
- L37: `                    (procs[i].priority == bestPriority && procs[i].arrival < procs[chosen].arrival)) {` — Track best (lowest numeric) priority among ready processes.
- L38: `                    bestPriority = procs[i].priority;` — Track best (lowest numeric) priority among ready processes.
- L39: `                    chosen = i;` — Execute this statement as part of the algorithm or UI flow.
- L40: `                }` — Close the current scope block.
- L41: `            }` — Close the current scope block.
- L42: `        }` — Close the current scope block.
- L43: `` — Blank line for readability and separation.
- L44: `        if (chosen == -1) {` — Conditional check to choose a branch.
- L45: `            int nextArrival = 1e9;` — Execute this statement as part of the algorithm or UI flow.
- L46: `            for (int i = 0; i < n; ++i) {` — Loop over a range of values.
- L47: `                if (!done[i]) nextArrival = std::min(nextArrival, procs[i].arrival);` — Conditional check to choose a branch.
- L48: `            }` — Close the current scope block.
- L49: `            gantt.push_back({-1, time, nextArrival});` — Execute this statement as part of the algorithm or UI flow.
- L50: `            time = nextArrival;` — Execute this statement as part of the algorithm or UI flow.
- L51: `            continue;` — Execute this statement as part of the algorithm or UI flow.
- L52: `        }` — Close the current scope block.
- L53: `` — Blank line for readability and separation.
- L54: `        int start = time;` — Execute this statement as part of the algorithm or UI flow.
- L55: `        response[chosen] = start - procs[chosen].arrival;` — Execute this statement as part of the algorithm or UI flow.
- L56: `        time += procs[chosen].burst;` — Execute this statement as part of the algorithm or UI flow.
- L57: `        completion[chosen] = time;` — Execute this statement as part of the algorithm or UI flow.
- L58: `        turnaround[chosen] = completion[chosen] - procs[chosen].arrival;` — Execute this statement as part of the algorithm or UI flow.
- L59: `        waiting[chosen] = turnaround[chosen] - procs[chosen].burst;` — Execute this statement as part of the algorithm or UI flow.
- L60: `        done[chosen] = true;` — Execute this statement as part of the algorithm or UI flow.
- L61: `        completed++;` — Execute this statement as part of the algorithm or UI flow.
- L62: `        gantt.push_back({procs[chosen].pid, start, time});` — Execute this statement as part of the algorithm or UI flow.
- L63: `` — Blank line for readability and separation.
- L64: `        std::cout << "Step: P" << procs[chosen].pid << " runs from " << start << " to " << time << "\n";` — Print formatted output to the console.
- L65: `    }` — Close the current scope block.
- L66: `` — Blank line for readability and separation.
- L67: `    ScheduleResult result{procs, completion, waiting, turnaround, response, gantt, computeAverages(waiting, turnaround, response)};` — Execute this statement as part of the algorithm or UI flow.
- L68: `    return result;` — Return from the function.
- L69: `}` — Close the current scope block.

## File: cpu_scheduling\round_robin.cpp
- L1: `#include <algorithm>` — Include required standard or project header so symbols compile.
- L2: `#include <iostream>` — Include required standard or project header so symbols compile.
- L3: `#include <queue>` — Include required standard or project header so symbols compile.
- L4: `#include <vector>` — Include required standard or project header so symbols compile.
- L5: `` — Blank line for readability and separation.
- L6: `#include "cpu_utils.h"` — Include required standard or project header so symbols compile.
- L7: `` — Blank line for readability and separation.
- L8: `namespace ui {` — Begin namespace scope for UI helpers.
- L9: `    int readInt(const std::string& prompt, int minValue, int maxValue);` — Execute this statement as part of the algorithm or UI flow.
- L10: `    void printSection(const std::string& title);` — Define a function that performs actions without returning a value.
- L11: `}` — Close the current scope block.
- L12: `` — Blank line for readability and separation.
- L13: `ScheduleResult runRoundRobin() {` — Execute this statement as part of the algorithm or UI flow.
- L14: `    ui::printSection("Round Robin Scheduling");` — Execute this statement as part of the algorithm or UI flow.
- L15: `    int n = ui::readInt("Enter number of processes (1-10): ", 1, 10);` — Execute this statement as part of the algorithm or UI flow.
- L16: `    int quantum = ui::readInt("Enter time quantum (1-10): ", 1, 10);` — Time slice for each process in Round Robin.
- L17: `` — Blank line for readability and separation.
- L18: `    std::vector<Process> procs(n);` — Execute this statement as part of the algorithm or UI flow.
- L19: `    for (int i = 0; i < n; ++i) {` — Loop over a range of values.
- L20: `        procs[i].pid = i + 1;` — Execute this statement as part of the algorithm or UI flow.
- L21: `        procs[i].arrival = ui::readInt("Arrival time P" + std::to_string(i + 1) + " (0-50): ", 0, 50);` — Execute this statement as part of the algorithm or UI flow.
- L22: `        procs[i].burst = ui::readInt("Burst time P" + std::to_string(i + 1) + " (1-50): ", 1, 50);` — Execute this statement as part of the algorithm or UI flow.
- L23: `        procs[i].priority = 0;` — Execute this statement as part of the algorithm or UI flow.
- L24: `    }` — Close the current scope block.
- L25: `` — Blank line for readability and separation.
- L26: `    std::vector<int> remaining(n), completion(n, 0), waiting(n, 0), turnaround(n, 0), response(n, -1);` — Track remaining time to know when a process finishes.
- L27: `    for (int i = 0; i < n; ++i) remaining[i] = procs[i].burst;` — Track remaining time to know when a process finishes.
- L28: `` — Blank line for readability and separation.
- L29: `    std::vector<int> order(n);` — Execute this statement as part of the algorithm or UI flow.
- L30: `    for (int i = 0; i < n; ++i) order[i] = i;` — Loop over a range of values.
- L31: `    std::sort(order.begin(), order.end(), [&](int a, int b) {` — Execute this statement as part of the algorithm or UI flow.
- L32: `        if (procs[a].arrival == procs[b].arrival) return procs[a].pid < procs[b].pid;` — Conditional check to choose a branch.
- L33: `        return procs[a].arrival < procs[b].arrival;` — Return from the function.
- L34: `    });` — End a scope block.
- L35: `` — Blank line for readability and separation.
- L36: `    std::queue<int> q;` — Ready queue for rotating processes in RR.
- L37: `    int time = 0;` — Execute this statement as part of the algorithm or UI flow.
- L38: `    int nextIndex = 0;` — Execute this statement as part of the algorithm or UI flow.
- L39: `    int completed = 0;` — Execute this statement as part of the algorithm or UI flow.
- L40: `    std::vector<GanttEntry> gantt;` — Execute this statement as part of the algorithm or UI flow.
- L41: `` — Blank line for readability and separation.
- L42: `    auto pushArrivals = [&]() {` — Execute this statement as part of the algorithm or UI flow.
- L43: `        while (nextIndex < n && procs[order[nextIndex]].arrival <= time) {` — Loop while a condition remains true.
- L44: `            q.push(order[nextIndex]);` — Execute this statement as part of the algorithm or UI flow.
- L45: `            nextIndex++;` — Execute this statement as part of the algorithm or UI flow.
- L46: `        }` — Close the current scope block.
- L47: `    };` — End a scope block.
- L48: `` — Blank line for readability and separation.
- L49: `    if (order.size() > 0) {` — Conditional check to choose a branch.
- L50: `        time = std::max(time, procs[order[0]].arrival);` — Execute this statement as part of the algorithm or UI flow.
- L51: `        pushArrivals();` — Execute this statement as part of the algorithm or UI flow.
- L52: `    }` — Close the current scope block.
- L53: `` — Blank line for readability and separation.
- L54: `    while (completed < n) {` — Loop while a condition remains true.
- L55: `        if (q.empty()) {` — Conditional check to choose a branch.
- L56: `            int nextArrival = procs[order[nextIndex]].arrival;` — Execute this statement as part of the algorithm or UI flow.
- L57: `            gantt.push_back({-1, time, nextArrival});` — Execute this statement as part of the algorithm or UI flow.
- L58: `            time = nextArrival;` — Execute this statement as part of the algorithm or UI flow.
- L59: `            pushArrivals();` — Execute this statement as part of the algorithm or UI flow.
- L60: `            continue;` — Execute this statement as part of the algorithm or UI flow.
- L61: `        }` — Close the current scope block.
- L62: `` — Blank line for readability and separation.
- L63: `        int i = q.front();` — Execute this statement as part of the algorithm or UI flow.
- L64: `        q.pop();` — Execute this statement as part of the algorithm or UI flow.
- L65: `        if (response[i] == -1) response[i] = time - procs[i].arrival;` — Conditional check to choose a branch.
- L66: `` — Blank line for readability and separation.
- L67: `        int run = std::min(quantum, remaining[i]);` — Time slice for each process in Round Robin.
- L68: `        int start = time;` — Execute this statement as part of the algorithm or UI flow.
- L69: `        time += run;` — Execute this statement as part of the algorithm or UI flow.
- L70: `        remaining[i] -= run;` — Track remaining time to know when a process finishes.
- L71: `        gantt.push_back({procs[i].pid, start, time});` — Execute this statement as part of the algorithm or UI flow.
- L72: `` — Blank line for readability and separation.
- L73: `        std::cout << "Step: P" << procs[i].pid << " runs from " << start << " to " << time` — Print formatted output to the console.
- L74: `                  << " (remaining " << remaining[i] << ")\n";` — Track remaining time to know when a process finishes.
- L75: `` — Blank line for readability and separation.
- L76: `        pushArrivals();` — Execute this statement as part of the algorithm or UI flow.
- L77: `        if (remaining[i] > 0) {` — Track remaining time to know when a process finishes.
- L78: `            q.push(i);` — Execute this statement as part of the algorithm or UI flow.
- L79: `        } else {` — End a scope block.
- L80: `            completion[i] = time;` — Execute this statement as part of the algorithm or UI flow.
- L81: `            turnaround[i] = completion[i] - procs[i].arrival;` — Execute this statement as part of the algorithm or UI flow.
- L82: `            waiting[i] = turnaround[i] - procs[i].burst;` — Execute this statement as part of the algorithm or UI flow.
- L83: `            completed++;` — Execute this statement as part of the algorithm or UI flow.
- L84: `        }` — Close the current scope block.
- L85: `    }` — Close the current scope block.
- L86: `` — Blank line for readability and separation.
- L87: `    ScheduleResult result{procs, completion, waiting, turnaround, response, gantt, computeAverages(waiting, turnaround, response)};` — Execute this statement as part of the algorithm or UI flow.
- L88: `    return result;` — Return from the function.
- L89: `}` — Close the current scope block.

## File: cpu_scheduling\sjf.cpp
- L1: `#include <algorithm>` — Include required standard or project header so symbols compile.
- L2: `#include <iostream>` — Include required standard or project header so symbols compile.
- L3: `#include <queue>` — Include required standard or project header so symbols compile.
- L4: `#include <vector>` — Include required standard or project header so symbols compile.
- L5: `` — Blank line for readability and separation.
- L6: `#include "cpu_utils.h"` — Include required standard or project header so symbols compile.
- L7: `` — Blank line for readability and separation.
- L8: `namespace ui {` — Begin namespace scope for UI helpers.
- L9: `    int readInt(const std::string& prompt, int minValue, int maxValue);` — Execute this statement as part of the algorithm or UI flow.
- L10: `    void printSection(const std::string& title);` — Define a function that performs actions without returning a value.
- L11: `}` — Close the current scope block.
- L12: `` — Blank line for readability and separation.
- L13: `ScheduleResult runSJFNonPreemptive() {` — Execute this statement as part of the algorithm or UI flow.
- L14: `    ui::printSection("SJF Non-Preemptive");` — Execute this statement as part of the algorithm or UI flow.
- L15: `    int n = ui::readInt("Enter number of processes (1-10): ", 1, 10);` — Execute this statement as part of the algorithm or UI flow.
- L16: `` — Blank line for readability and separation.
- L17: `    std::vector<Process> procs(n);` — Execute this statement as part of the algorithm or UI flow.
- L18: `    for (int i = 0; i < n; ++i) {` — Loop over a range of values.
- L19: `        procs[i].pid = i + 1;` — Execute this statement as part of the algorithm or UI flow.
- L20: `        procs[i].arrival = ui::readInt("Arrival time P" + std::to_string(i + 1) + " (0-50): ", 0, 50);` — Execute this statement as part of the algorithm or UI flow.
- L21: `        procs[i].burst = ui::readInt("Burst time P" + std::to_string(i + 1) + " (1-50): ", 1, 50);` — Execute this statement as part of the algorithm or UI flow.
- L22: `        procs[i].priority = 0;` — Execute this statement as part of the algorithm or UI flow.
- L23: `    }` — Close the current scope block.
- L24: `` — Blank line for readability and separation.
- L25: `    std::vector<int> completion(n, 0), waiting(n, 0), turnaround(n, 0), response(n, 0);` — Execute this statement as part of the algorithm or UI flow.
- L26: `    std::vector<GanttEntry> gantt;` — Execute this statement as part of the algorithm or UI flow.
- L27: `` — Blank line for readability and separation.
- L28: `    int time = 0;` — Execute this statement as part of the algorithm or UI flow.
- L29: `    int completed = 0;` — Execute this statement as part of the algorithm or UI flow.
- L30: `    std::vector<bool> done(n, false);` — Execute this statement as part of the algorithm or UI flow.
- L31: `` — Blank line for readability and separation.
- L32: `    while (completed < n) {` — Loop while a condition remains true.
- L33: `        int chosen = -1;` — Execute this statement as part of the algorithm or UI flow.
- L34: `        int minBurst = 1e9;` — Track the smallest burst to implement SJF selection.
- L35: `        for (int i = 0; i < n; ++i) {` — Loop over a range of values.
- L36: `            if (!done[i] && procs[i].arrival <= time) {` — Conditional check to choose a branch.
- L37: `                if (procs[i].burst < minBurst ||` — Track the smallest burst to implement SJF selection.
- L38: `                    (procs[i].burst == minBurst && procs[i].arrival < procs[chosen].arrival)) {` — Track the smallest burst to implement SJF selection.
- L39: `                    minBurst = procs[i].burst;` — Track the smallest burst to implement SJF selection.
- L40: `                    chosen = i;` — Execute this statement as part of the algorithm or UI flow.
- L41: `                }` — Close the current scope block.
- L42: `            }` — Close the current scope block.
- L43: `        }` — Close the current scope block.
- L44: `` — Blank line for readability and separation.
- L45: `        if (chosen == -1) {` — Conditional check to choose a branch.
- L46: `            int nextArrival = 1e9;` — Execute this statement as part of the algorithm or UI flow.
- L47: `            for (int i = 0; i < n; ++i) {` — Loop over a range of values.
- L48: `                if (!done[i]) nextArrival = std::min(nextArrival, procs[i].arrival);` — Conditional check to choose a branch.
- L49: `            }` — Close the current scope block.
- L50: `            gantt.push_back({-1, time, nextArrival});` — Execute this statement as part of the algorithm or UI flow.
- L51: `            time = nextArrival;` — Execute this statement as part of the algorithm or UI flow.
- L52: `            continue;` — Execute this statement as part of the algorithm or UI flow.
- L53: `        }` — Close the current scope block.
- L54: `` — Blank line for readability and separation.
- L55: `        int start = time;` — Execute this statement as part of the algorithm or UI flow.
- L56: `        response[chosen] = start - procs[chosen].arrival;` — Execute this statement as part of the algorithm or UI flow.
- L57: `        time += procs[chosen].burst;` — Execute this statement as part of the algorithm or UI flow.
- L58: `        completion[chosen] = time;` — Execute this statement as part of the algorithm or UI flow.
- L59: `        turnaround[chosen] = completion[chosen] - procs[chosen].arrival;` — Execute this statement as part of the algorithm or UI flow.
- L60: `        waiting[chosen] = turnaround[chosen] - procs[chosen].burst;` — Execute this statement as part of the algorithm or UI flow.
- L61: `        done[chosen] = true;` — Execute this statement as part of the algorithm or UI flow.
- L62: `        completed++;` — Execute this statement as part of the algorithm or UI flow.
- L63: `        gantt.push_back({procs[chosen].pid, start, time});` — Execute this statement as part of the algorithm or UI flow.
- L64: `` — Blank line for readability and separation.
- L65: `        std::cout << "Step: P" << procs[chosen].pid << " runs from " << start << " to " << time << "\n";` — Print formatted output to the console.
- L66: `    }` — Close the current scope block.
- L67: `` — Blank line for readability and separation.
- L68: `    ScheduleResult result{procs, completion, waiting, turnaround, response, gantt, computeAverages(waiting, turnaround, response)};` — Execute this statement as part of the algorithm or UI flow.
- L69: `    return result;` — Return from the function.
- L70: `}` — Close the current scope block.
- L71: `` — Blank line for readability and separation.
- L72: `ScheduleResult runSJFPreemptive() {` — Execute this statement as part of the algorithm or UI flow.
- L73: `    ui::printSection("SJF Preemptive (SRTF)");` — Execute this statement as part of the algorithm or UI flow.
- L74: `    int n = ui::readInt("Enter number of processes (1-10): ", 1, 10);` — Execute this statement as part of the algorithm or UI flow.
- L75: `` — Blank line for readability and separation.
- L76: `    std::vector<Process> procs(n);` — Execute this statement as part of the algorithm or UI flow.
- L77: `    for (int i = 0; i < n; ++i) {` — Loop over a range of values.
- L78: `        procs[i].pid = i + 1;` — Execute this statement as part of the algorithm or UI flow.
- L79: `        procs[i].arrival = ui::readInt("Arrival time P" + std::to_string(i + 1) + " (0-50): ", 0, 50);` — Execute this statement as part of the algorithm or UI flow.
- L80: `        procs[i].burst = ui::readInt("Burst time P" + std::to_string(i + 1) + " (1-50): ", 1, 50);` — Execute this statement as part of the algorithm or UI flow.
- L81: `        procs[i].priority = 0;` — Execute this statement as part of the algorithm or UI flow.
- L82: `    }` — Close the current scope block.
- L83: `` — Blank line for readability and separation.
- L84: `    std::vector<int> remaining(n), completion(n, 0), waiting(n, 0), turnaround(n, 0), response(n, -1);` — Execute this statement as part of the algorithm or UI flow.
- L85: `    for (int i = 0; i < n; ++i) remaining[i] = procs[i].burst;` — Maintain remaining burst times for preemption.
- L86: `` — Blank line for readability and separation.
- L87: `    int time = 0;` — Execute this statement as part of the algorithm or UI flow.
- L88: `    int completed = 0;` — Execute this statement as part of the algorithm or UI flow.
- L89: `    std::vector<GanttEntry> gantt;` — Execute this statement as part of the algorithm or UI flow.
- L90: `    int current = -1;` — Execute this statement as part of the algorithm or UI flow.
- L91: `    int segmentStart = 0;` — Execute this statement as part of the algorithm or UI flow.
- L92: `` — Blank line for readability and separation.
- L93: `    while (completed < n) {` — Loop while a condition remains true.
- L94: `        int chosen = -1;` — Execute this statement as part of the algorithm or UI flow.
- L95: `        int minRemaining = 1e9;` — Track the smallest remaining time for SRTF.
- L96: `        for (int i = 0; i < n; ++i) {` — Loop over a range of values.
- L97: `            if (procs[i].arrival <= time && remaining[i] > 0) {` — Maintain remaining burst times for preemption.
- L98: `                if (remaining[i] < minRemaining ||` — Track the smallest remaining time for SRTF.
- L99: `                    (remaining[i] == minRemaining && procs[i].arrival < procs[chosen].arrival)) {` — Track the smallest remaining time for SRTF.
- L100: `                    minRemaining = remaining[i];` — Track the smallest remaining time for SRTF.
- L101: `                    chosen = i;` — Execute this statement as part of the algorithm or UI flow.
- L102: `                }` — Close the current scope block.
- L103: `            }` — Close the current scope block.
- L104: `        }` — Close the current scope block.
- L105: `` — Blank line for readability and separation.
- L106: `        if (chosen == -1) {` — Conditional check to choose a branch.
- L107: `            if (current != -1) {` — Conditional check to choose a branch.
- L108: `                gantt.push_back({procs[current].pid, segmentStart, time});` — Execute this statement as part of the algorithm or UI flow.
- L109: `                current = -1;` — Execute this statement as part of the algorithm or UI flow.
- L110: `            }` — Close the current scope block.
- L111: `            int nextArrival = 1e9;` — Execute this statement as part of the algorithm or UI flow.
- L112: `            for (int i = 0; i < n; ++i) {` — Loop over a range of values.
- L113: `                if (remaining[i] > 0) nextArrival = std::min(nextArrival, procs[i].arrival);` — Maintain remaining burst times for preemption.
- L114: `            }` — Close the current scope block.
- L115: `            gantt.push_back({-1, time, nextArrival});` — Execute this statement as part of the algorithm or UI flow.
- L116: `            time = nextArrival;` — Execute this statement as part of the algorithm or UI flow.
- L117: `            continue;` — Execute this statement as part of the algorithm or UI flow.
- L118: `        }` — Close the current scope block.
- L119: `` — Blank line for readability and separation.
- L120: `        if (current != chosen) {` — Conditional check to choose a branch.
- L121: `            if (current != -1) {` — Conditional check to choose a branch.
- L122: `                gantt.push_back({procs[current].pid, segmentStart, time});` — Execute this statement as part of the algorithm or UI flow.
- L123: `            }` — Close the current scope block.
- L124: `            current = chosen;` — Execute this statement as part of the algorithm or UI flow.
- L125: `            segmentStart = time;` — Execute this statement as part of the algorithm or UI flow.
- L126: `            if (response[chosen] == -1) response[chosen] = time - procs[chosen].arrival;` — Conditional check to choose a branch.
- L127: `        }` — Close the current scope block.
- L128: `` — Blank line for readability and separation.
- L129: `        remaining[chosen]--;` — Maintain remaining burst times for preemption.
- L130: `        time++;` — Execute this statement as part of the algorithm or UI flow.
- L131: `` — Blank line for readability and separation.
- L132: `        if (remaining[chosen] == 0) {` — Maintain remaining burst times for preemption.
- L133: `            completion[chosen] = time;` — Execute this statement as part of the algorithm or UI flow.
- L134: `            turnaround[chosen] = completion[chosen] - procs[chosen].arrival;` — Execute this statement as part of the algorithm or UI flow.
- L135: `            waiting[chosen] = turnaround[chosen] - procs[chosen].burst;` — Execute this statement as part of the algorithm or UI flow.
- L136: `            completed++;` — Execute this statement as part of the algorithm or UI flow.
- L137: `            gantt.push_back({procs[chosen].pid, segmentStart, time});` — Execute this statement as part of the algorithm or UI flow.
- L138: `            current = -1;` — Execute this statement as part of the algorithm or UI flow.
- L139: `` — Blank line for readability and separation.
- L140: `            std::cout << "Step: P" << procs[chosen].pid << " completes at " << time << "\n";` — Print formatted output to the console.
- L141: `        }` — Close the current scope block.
- L142: `    }` — Close the current scope block.
- L143: `` — Blank line for readability and separation.
- L144: `    ScheduleResult result{procs, completion, waiting, turnaround, response, gantt, computeAverages(waiting, turnaround, response)};` — Execute this statement as part of the algorithm or UI flow.
- L145: `    return result;` — Return from the function.
- L146: `}` — Close the current scope block.

## File: disk_scheduling\cscan.cpp
- L1: `#include <algorithm>` — Include required standard or project header so symbols compile.
- L2: `#include <cmath>` — Include required standard or project header so symbols compile.
- L3: `#include <iostream>` — Include required standard or project header so symbols compile.
- L4: `#include <string>` — Include required standard or project header so symbols compile.
- L5: `#include <vector>` — Include required standard or project header so symbols compile.
- L6: `` — Blank line for readability and separation.
- L7: `namespace ui {` — Begin namespace scope for UI helpers.
- L8: `    int readInt(const std::string& prompt, int minValue, int maxValue);` — Execute this statement as part of the algorithm or UI flow.
- L9: `    void printSection(const std::string& title);` — Define a function that performs actions without returning a value.
- L10: `}` — Close the current scope block.
- L11: `` — Blank line for readability and separation.
- L12: `void runCSCAN() {` — Define a function that performs actions without returning a value.
- L13: `    ui::printSection("Disk Scheduling - C-SCAN");` — Execute this statement as part of the algorithm or UI flow.
- L14: `    int n = ui::readInt("Enter number of requests (1-20): ", 1, 20);` — Execute this statement as part of the algorithm or UI flow.
- L15: `    int diskSize = ui::readInt("Enter disk size (tracks, 1-5000): ", 1, 5000);` — Execute this statement as part of the algorithm or UI flow.
- L16: `    int head = ui::readInt("Enter initial head position (0-" + std::to_string(diskSize - 1) + "): ", 0, diskSize - 1);` — Execute this statement as part of the algorithm or UI flow.
- L17: `` — Blank line for readability and separation.
- L18: `    std::vector<int> req(n);` — Execute this statement as part of the algorithm or UI flow.
- L19: `    for (int i = 0; i < n; ++i) {` — Loop over a range of values.
- L20: `        req[i] = ui::readInt("Request " + std::to_string(i + 1) + " (0-" + std::to_string(diskSize - 1) + "): ", 0, diskSize - 1);` — Execute this statement as part of the algorithm or UI flow.
- L21: `    }` — Close the current scope block.
- L22: `    std::sort(req.begin(), req.end());` — Execute this statement as part of the algorithm or UI flow.
- L23: `` — Blank line for readability and separation.
- L24: `    std::vector<int> sequence; // Service order for C-SCAN (circular).` — Build circular service order for C-SCAN.
- L25: `    for (int r : req) if (r >= head) sequence.push_back(r);` — Build circular service order for C-SCAN.
- L26: `    if (head != diskSize - 1) sequence.push_back(diskSize - 1);` — Build circular service order for C-SCAN.
- L27: `    sequence.push_back(0); // wrap to start without servicing in between` — Build circular service order for C-SCAN.
- L28: `    for (int r : req) if (r < head) sequence.push_back(r);` — Build circular service order for C-SCAN.
- L29: `` — Blank line for readability and separation.
- L30: `    int total = 0;` — Execute this statement as part of the algorithm or UI flow.
- L31: `    std::cout << "\nSequence: " << head;` — Build circular service order for C-SCAN.
- L32: `    for (int r : sequence) {` — Build circular service order for C-SCAN.
- L33: `        total += std::abs(r - head);` — Execute this statement as part of the algorithm or UI flow.
- L34: `        std::cout << " -> " << r << " (move " << std::abs(r - head) << ")";` — Print formatted output to the console.
- L35: `        head = r;` — Execute this statement as part of the algorithm or UI flow.
- L36: `    }` — Close the current scope block.
- L37: `` — Blank line for readability and separation.
- L38: `    std::cout << "\nTotal Head Movement: " << total << "\n";` — Print formatted output to the console.
- L39: `}` — Close the current scope block.

## File: disk_scheduling\fcfs_disk.cpp
- L1: `#include <cmath>` — Include required standard or project header so symbols compile.
- L2: `#include <iostream>` — Include required standard or project header so symbols compile.
- L3: `#include <string>` — Include required standard or project header so symbols compile.
- L4: `#include <vector>` — Include required standard or project header so symbols compile.
- L5: `` — Blank line for readability and separation.
- L6: `namespace ui {` — Begin namespace scope for UI helpers.
- L7: `    int readInt(const std::string& prompt, int minValue, int maxValue);` — Execute this statement as part of the algorithm or UI flow.
- L8: `    void printSection(const std::string& title);` — Define a function that performs actions without returning a value.
- L9: `}` — Close the current scope block.
- L10: `` — Blank line for readability and separation.
- L11: `void runFCFSDisk() {` — Define a function that performs actions without returning a value.
- L12: `    ui::printSection("Disk Scheduling - FCFS");` — Execute this statement as part of the algorithm or UI flow.
- L13: `    int n = ui::readInt("Enter number of requests (1-20): ", 1, 20);` — Execute this statement as part of the algorithm or UI flow.
- L14: `    int diskSize = ui::readInt("Enter disk size (tracks, 1-5000): ", 1, 5000);` — Execute this statement as part of the algorithm or UI flow.
- L15: `    int head = ui::readInt("Enter initial head position (0-" + std::to_string(diskSize - 1) + "): ", 0, diskSize - 1);` — Execute this statement as part of the algorithm or UI flow.
- L16: `` — Blank line for readability and separation.
- L17: `    std::vector<int> req(n);` — Execute this statement as part of the algorithm or UI flow.
- L18: `    for (int i = 0; i < n; ++i) {` — Loop over a range of values.
- L19: `        req[i] = ui::readInt("Request " + std::to_string(i + 1) + " (0-" + std::to_string(diskSize - 1) + "): ", 0, diskSize - 1);` — Execute this statement as part of the algorithm or UI flow.
- L20: `    }` — Close the current scope block.
- L21: `` — Blank line for readability and separation.
- L22: `    int total = 0; // Total head movement.` — Execute this statement as part of the algorithm or UI flow.
- L23: `    std::cout << "\nSequence: " << head;` — Print formatted output to the console.
- L24: `    for (int i = 0; i < n; ++i) {` — Loop over a range of values.
- L25: `        // FCFS services requests in given order.` — Comment added to explain intent or algorithm detail.
- L26: `        total += std::abs(req[i] - head);` — Execute this statement as part of the algorithm or UI flow.
- L27: `        std::cout << " -> " << req[i] << " (move " << std::abs(req[i] - head) << ")";` — Print formatted output to the console.
- L28: `        head = req[i];` — Execute this statement as part of the algorithm or UI flow.
- L29: `    }` — Close the current scope block.
- L30: `    std::cout << "\nTotal Head Movement: " << total << "\n";` — Print formatted output to the console.
- L31: `}` — Close the current scope block.

## File: disk_scheduling\scan.cpp
- L1: `#include <algorithm>` — Include required standard or project header so symbols compile.
- L2: `#include <cmath>` — Include required standard or project header so symbols compile.
- L3: `#include <iostream>` — Include required standard or project header so symbols compile.
- L4: `#include <string>` — Include required standard or project header so symbols compile.
- L5: `#include <vector>` — Include required standard or project header so symbols compile.
- L6: `` — Blank line for readability and separation.
- L7: `namespace ui {` — Begin namespace scope for UI helpers.
- L8: `    int readInt(const std::string& prompt, int minValue, int maxValue);` — Execute this statement as part of the algorithm or UI flow.
- L9: `    void printSection(const std::string& title);` — Define a function that performs actions without returning a value.
- L10: `}` — Close the current scope block.
- L11: `` — Blank line for readability and separation.
- L12: `void runSCANDisk() {` — Define a function that performs actions without returning a value.
- L13: `    ui::printSection("Disk Scheduling - SCAN");` — Execute this statement as part of the algorithm or UI flow.
- L14: `    int n = ui::readInt("Enter number of requests (1-20): ", 1, 20);` — Execute this statement as part of the algorithm or UI flow.
- L15: `    int diskSize = ui::readInt("Enter disk size (tracks, 1-5000): ", 1, 5000);` — Execute this statement as part of the algorithm or UI flow.
- L16: `    int head = ui::readInt("Enter initial head position (0-" + std::to_string(diskSize - 1) + "): ", 0, diskSize - 1);` — Execute this statement as part of the algorithm or UI flow.
- L17: `    int dir = ui::readInt("Direction (0=left, 1=right): ", 0, 1);` — Direction determines scan movement (left/right).
- L18: `` — Blank line for readability and separation.
- L19: `    std::vector<int> req(n);` — Execute this statement as part of the algorithm or UI flow.
- L20: `    for (int i = 0; i < n; ++i) {` — Loop over a range of values.
- L21: `        req[i] = ui::readInt("Request " + std::to_string(i + 1) + " (0-" + std::to_string(diskSize - 1) + "): ", 0, diskSize - 1);` — Execute this statement as part of the algorithm or UI flow.
- L22: `    }` — Close the current scope block.
- L23: `    std::sort(req.begin(), req.end());` — Execute this statement as part of the algorithm or UI flow.
- L24: `` — Blank line for readability and separation.
- L25: `    std::vector<int> sequence; // Order of service based on scan direction.` — Build service order for SCAN algorithm.
- L26: `    if (dir == 1) {` — Direction determines scan movement (left/right).
- L27: `        // Move right: service >= head, go to end, then reverse.` — Comment added to explain intent or algorithm detail.
- L28: `        for (int r : req) if (r >= head) sequence.push_back(r);` — Build service order for SCAN algorithm.
- L29: `        if (head != diskSize - 1) sequence.push_back(diskSize - 1);` — Build service order for SCAN algorithm.
- L30: `        for (int i = static_cast<int>(req.size()) - 1; i >= 0; --i) {` — Loop over a range of values.
- L31: `            if (req[i] < head) sequence.push_back(req[i]);` — Build service order for SCAN algorithm.
- L32: `        }` — Close the current scope block.
- L33: `    } else {` — End a scope block.
- L34: `        // Move left: service <= head, go to 0, then reverse.` — Comment added to explain intent or algorithm detail.
- L35: `        for (int i = static_cast<int>(req.size()) - 1; i >= 0; --i) {` — Loop over a range of values.
- L36: `            if (req[i] <= head) sequence.push_back(req[i]);` — Build service order for SCAN algorithm.
- L37: `        }` — Close the current scope block.
- L38: `        if (head != 0) sequence.push_back(0);` — Build service order for SCAN algorithm.
- L39: `        for (int r : req) if (r > head) sequence.push_back(r);` — Build service order for SCAN algorithm.
- L40: `    }` — Close the current scope block.
- L41: `` — Blank line for readability and separation.
- L42: `    int total = 0;` — Execute this statement as part of the algorithm or UI flow.
- L43: `    std::cout << "\nSequence: " << head;` — Build service order for SCAN algorithm.
- L44: `    for (int r : sequence) {` — Build service order for SCAN algorithm.
- L45: `        total += std::abs(r - head);` — Execute this statement as part of the algorithm or UI flow.
- L46: `        std::cout << " -> " << r << " (move " << std::abs(r - head) << ")";` — Print formatted output to the console.
- L47: `        head = r;` — Execute this statement as part of the algorithm or UI flow.
- L48: `    }` — Close the current scope block.
- L49: `    std::cout << "\nTotal Head Movement: " << total << "\n";` — Print formatted output to the console.
- L50: `}` — Close the current scope block.

## File: disk_scheduling\sstf.cpp
- L1: `#include <cmath>` — Include required standard or project header so symbols compile.
- L2: `#include <iostream>` — Include required standard or project header so symbols compile.
- L3: `#include <string>` — Include required standard or project header so symbols compile.
- L4: `#include <limits>` — Include required standard or project header so symbols compile.
- L5: `#include <vector>` — Include required standard or project header so symbols compile.
- L6: `` — Blank line for readability and separation.
- L7: `namespace ui {` — Begin namespace scope for UI helpers.
- L8: `    int readInt(const std::string& prompt, int minValue, int maxValue);` — Execute this statement as part of the algorithm or UI flow.
- L9: `    void printSection(const std::string& title);` — Define a function that performs actions without returning a value.
- L10: `}` — Close the current scope block.
- L11: `` — Blank line for readability and separation.
- L12: `void runSSTFDisk() {` — Define a function that performs actions without returning a value.
- L13: `    ui::printSection("Disk Scheduling - SSTF");` — Execute this statement as part of the algorithm or UI flow.
- L14: `    int n = ui::readInt("Enter number of requests (1-20): ", 1, 20);` — Execute this statement as part of the algorithm or UI flow.
- L15: `    int diskSize = ui::readInt("Enter disk size (tracks, 1-5000): ", 1, 5000);` — Execute this statement as part of the algorithm or UI flow.
- L16: `    int head = ui::readInt("Enter initial head position (0-" + std::to_string(diskSize - 1) + "): ", 0, diskSize - 1);` — Execute this statement as part of the algorithm or UI flow.
- L17: `` — Blank line for readability and separation.
- L18: `    std::vector<int> req(n);` — Execute this statement as part of the algorithm or UI flow.
- L19: `    for (int i = 0; i < n; ++i) {` — Loop over a range of values.
- L20: `        req[i] = ui::readInt("Request " + std::to_string(i + 1) + " (0-" + std::to_string(diskSize - 1) + "): ", 0, diskSize - 1);` — Execute this statement as part of the algorithm or UI flow.
- L21: `    }` — Close the current scope block.
- L22: `` — Blank line for readability and separation.
- L23: `    std::vector<bool> done(n, false); // Tracks which requests are already serviced.` — Execute this statement as part of the algorithm or UI flow.
- L24: `    int total = 0;` — Execute this statement as part of the algorithm or UI flow.
- L25: `    std::cout << "\nSequence: " << head;` — Print formatted output to the console.
- L26: `` — Blank line for readability and separation.
- L27: `    for (int completed = 0; completed < n; ++completed) {` — Loop over a range of values.
- L28: `        int best = -1;` — Execute this statement as part of the algorithm or UI flow.
- L29: `        int bestDist = std::numeric_limits<int>::max();` — Execute this statement as part of the algorithm or UI flow.
- L30: `        for (int i = 0; i < n; ++i) {` — Loop over a range of values.
- L31: `            if (!done[i]) {` — Conditional check to choose a branch.
- L32: `                // SSTF picks the nearest request to the current head.` — Comment added to explain intent or algorithm detail.
- L33: `                int dist = std::abs(req[i] - head);` — Execute this statement as part of the algorithm or UI flow.
- L34: `                if (dist < bestDist) {` — Conditional check to choose a branch.
- L35: `                    bestDist = dist;` — Execute this statement as part of the algorithm or UI flow.
- L36: `                    best = i;` — Execute this statement as part of the algorithm or UI flow.
- L37: `                }` — Close the current scope block.
- L38: `            }` — Close the current scope block.
- L39: `        }` — Close the current scope block.
- L40: `        done[best] = true;` — Execute this statement as part of the algorithm or UI flow.
- L41: `        total += bestDist;` — Execute this statement as part of the algorithm or UI flow.
- L42: `        std::cout << " -> " << req[best] << " (move " << bestDist << ")";` — Print formatted output to the console.
- L43: `        head = req[best];` — Execute this statement as part of the algorithm or UI flow.
- L44: `    }` — Close the current scope block.
- L45: `` — Blank line for readability and separation.
- L46: `    std::cout << "\nTotal Head Movement: " << total << "\n";` — Print formatted output to the console.
- L47: `}` — Close the current scope block.

## File: main.cpp
- L1: `#include <iostream>` — Include required standard or project header so symbols compile.
- L2: `#include <string>` — Include required standard or project header so symbols compile.
- L3: `#include <vector>` — Include required standard or project header so symbols compile.
- L4: `` — Blank line for readability and separation.
- L5: `#include "cpu_scheduling/cpu_utils.h"` — Include required standard or project header so symbols compile.
- L6: `` — Blank line for readability and separation.
- L7: `namespace ui {` — Begin namespace scope for UI helpers.
- L8: `    int readInt(const std::string& prompt, int minValue, int maxValue);` — Execute this statement as part of the algorithm or UI flow.
- L9: `    void printTitle(const std::string& title);` — Define a function that performs actions without returning a value.
- L10: `    void printSection(const std::string& title);` — Define a function that performs actions without returning a value.
- L11: `    void waitForEnter();` — Define a function that performs actions without returning a value.
- L12: `    std::string color(const std::string& text, const std::string& code);` — Execute this statement as part of the algorithm or UI flow.
- L13: `}` — Close the current scope block.
- L14: `` — Blank line for readability and separation.
- L15: `// CPU scheduling functions` — Comment added to explain intent or algorithm detail.
- L16: `ScheduleResult runFCFS();` — Execute this statement as part of the algorithm or UI flow.
- L17: `ScheduleResult runSJFNonPreemptive();` — Execute this statement as part of the algorithm or UI flow.
- L18: `ScheduleResult runSJFPreemptive();` — Execute this statement as part of the algorithm or UI flow.
- L19: `ScheduleResult runPriorityScheduling();` — Execute this statement as part of the algorithm or UI flow.
- L20: `ScheduleResult runRoundRobin();` — Execute this statement as part of the algorithm or UI flow.
- L21: `` — Blank line for readability and separation.
- L22: `// Memory management (allocation strategies)` — Comment added to explain intent or algorithm detail.
- L23: `void runFirstFit();` — Define a function that performs actions without returning a value.
- L24: `void runBestFit();` — Define a function that performs actions without returning a value.
- L25: `void runNextFit();` — Define a function that performs actions without returning a value.
- L26: `` — Blank line for readability and separation.
- L27: `// Page replacement` — Comment added to explain intent or algorithm detail.
- L28: `void runFIFOPageReplacement();` — Define a function that performs actions without returning a value.
- L29: `void runLRUPageReplacement();` — Define a function that performs actions without returning a value.
- L30: `void runOptimalPageReplacement();` — Define a function that performs actions without returning a value.
- L31: `` — Blank line for readability and separation.
- L32: `// Disk scheduling` — Comment added to explain intent or algorithm detail.
- L33: `void runFCFSDisk();` — Define a function that performs actions without returning a value.
- L34: `void runSSTFDisk();` — Define a function that performs actions without returning a value.
- L35: `void runSCANDisk();` — Define a function that performs actions without returning a value.
- L36: `void runCSCAN();` — Define a function that performs actions without returning a value.
- L37: `` — Blank line for readability and separation.
- L38: `static void showScheduleResult(const ScheduleResult& result) {` — Declare a file-local helper function.
- L39: `    printProcessTable(result.processes, result.completion, result.waiting, result.turnaround, result.response);` — Execute this statement as part of the algorithm or UI flow.
- L40: `    printMetrics(result.metrics);` — Execute this statement as part of the algorithm or UI flow.
- L41: `}` — Close the current scope block.
- L42: `` — Blank line for readability and separation.
- L43: `static void cpuSchedulingMenu() {` — Declare a file-local helper function.
- L44: `    while (true) {` — Loop while a condition remains true.
- L45: `        ui::printTitle("CPU Scheduling");` — Execute this statement as part of the algorithm or UI flow.
- L46: `        std::cout << "1. FCFS\n";` — Print formatted output to the console.
- L47: `        std::cout << "2. SJF Non-Preemptive\n";` — Print formatted output to the console.
- L48: `        std::cout << "3. SJF Preemptive (SRTF)\n";` — Print formatted output to the console.
- L49: `        std::cout << "4. Priority Scheduling\n";` — Print formatted output to the console.
- L50: `        std::cout << "5. Round Robin\n";` — Print formatted output to the console.
- L51: `        std::cout << "6. Back\n";` — Print formatted output to the console.
- L52: `` — Blank line for readability and separation.
- L53: `        int choice = ui::readInt("Select option: ", 1, 6);` — Execute this statement as part of the algorithm or UI flow.
- L54: `        if (choice == 6) return;` — Conditional check to choose a branch.
- L55: `` — Blank line for readability and separation.
- L56: `        ScheduleResult result;` — Execute this statement as part of the algorithm or UI flow.
- L57: `        switch (choice) {` — Execute this statement as part of the algorithm or UI flow.
- L58: `            case 1: result = runFCFS(); break;` — Execute this statement as part of the algorithm or UI flow.
- L59: `            case 2: result = runSJFNonPreemptive(); break;` — Execute this statement as part of the algorithm or UI flow.
- L60: `            case 3: result = runSJFPreemptive(); break;` — Execute this statement as part of the algorithm or UI flow.
- L61: `            case 4: result = runPriorityScheduling(); break;` — Execute this statement as part of the algorithm or UI flow.
- L62: `            case 5: result = runRoundRobin(); break;` — Execute this statement as part of the algorithm or UI flow.
- L63: `        }` — Close the current scope block.
- L64: `` — Blank line for readability and separation.
- L65: `        showScheduleResult(result);` — Execute this statement as part of the algorithm or UI flow.
- L66: `        ui::waitForEnter();` — Execute this statement as part of the algorithm or UI flow.
- L67: `    }` — Close the current scope block.
- L68: `}` — Close the current scope block.
- L69: `` — Blank line for readability and separation.
- L70: `static void memoryManagementMenu() {` — Declare a file-local helper function.
- L71: `    while (true) {` — Loop while a condition remains true.
- L72: `        ui::printTitle("Memory Management");` — Execute this statement as part of the algorithm or UI flow.
- L73: `        std::cout << "1. First Fit\n";` — Print formatted output to the console.
- L74: `        std::cout << "2. Best Fit\n";` — Print formatted output to the console.
- L75: `        std::cout << "3. Next Fit\n";` — Print formatted output to the console.
- L76: `        std::cout << "4. Back\n";` — Print formatted output to the console.
- L77: `        int choice = ui::readInt("Select option: ", 1, 4);` — Execute this statement as part of the algorithm or UI flow.
- L78: `        if (choice == 4) return;` — Conditional check to choose a branch.
- L79: `        if (choice == 1) runFirstFit();` — Conditional check to choose a branch.
- L80: `        if (choice == 2) runBestFit();` — Conditional check to choose a branch.
- L81: `        if (choice == 3) runNextFit();` — Conditional check to choose a branch.
- L82: `        ui::waitForEnter();` — Execute this statement as part of the algorithm or UI flow.
- L83: `    }` — Close the current scope block.
- L84: `}` — Close the current scope block.
- L85: `` — Blank line for readability and separation.
- L86: `static void pageReplacementMenu() {` — Declare a file-local helper function.
- L87: `    while (true) {` — Loop while a condition remains true.
- L88: `        ui::printTitle("Page Replacement");` — Execute this statement as part of the algorithm or UI flow.
- L89: `        std::cout << "1. FIFO\n";` — Print formatted output to the console.
- L90: `        std::cout << "2. LRU\n";` — Print formatted output to the console.
- L91: `        std::cout << "3. Optimal\n";` — Print formatted output to the console.
- L92: `        std::cout << "4. Back\n";` — Print formatted output to the console.
- L93: `        int choice = ui::readInt("Select option: ", 1, 4);` — Execute this statement as part of the algorithm or UI flow.
- L94: `        if (choice == 4) return;` — Conditional check to choose a branch.
- L95: `        if (choice == 1) runFIFOPageReplacement();` — Conditional check to choose a branch.
- L96: `        if (choice == 2) runLRUPageReplacement();` — Conditional check to choose a branch.
- L97: `        if (choice == 3) runOptimalPageReplacement();` — Conditional check to choose a branch.
- L98: `        ui::waitForEnter();` — Execute this statement as part of the algorithm or UI flow.
- L99: `    }` — Close the current scope block.
- L100: `}` — Close the current scope block.
- L101: `` — Blank line for readability and separation.
- L102: `static void diskSchedulingMenu() {` — Declare a file-local helper function.
- L103: `    while (true) {` — Loop while a condition remains true.
- L104: `        ui::printTitle("Disk Scheduling");` — Execute this statement as part of the algorithm or UI flow.
- L105: `        std::cout << "1. FCFS\n";` — Print formatted output to the console.
- L106: `        std::cout << "2. SSTF\n";` — Print formatted output to the console.
- L107: `        std::cout << "3. SCAN\n";` — Print formatted output to the console.
- L108: `        std::cout << "4. C-SCAN\n";` — Print formatted output to the console.
- L109: `        std::cout << "5. Back\n";` — Print formatted output to the console.
- L110: `        int choice = ui::readInt("Select option: ", 1, 5);` — Execute this statement as part of the algorithm or UI flow.
- L111: `        if (choice == 5) return;` — Conditional check to choose a branch.
- L112: `        if (choice == 1) runFCFSDisk();` — Conditional check to choose a branch.
- L113: `        if (choice == 2) runSSTFDisk();` — Conditional check to choose a branch.
- L114: `        if (choice == 3) runSCANDisk();` — Conditional check to choose a branch.
- L115: `        if (choice == 4) runCSCAN();` — Conditional check to choose a branch.
- L116: `        ui::waitForEnter();` — Execute this statement as part of the algorithm or UI flow.
- L117: `    }` — Close the current scope block.
- L118: `}` — Close the current scope block.
- L119: `` — Blank line for readability and separation.
- L120: `int main() {` — Program entry point where execution starts.
- L121: `    while (true) {` — Loop while a condition remains true.
- L122: `        ui::printTitle("Mini Operating System Simulator");` — Execute this statement as part of the algorithm or UI flow.
- L123: `        std::cout << "Developer: SABYASACHHI ROUTRAY\n";` — Print formatted output to the console.
- L124: `        std::cout << ui::color("Major Modules", "32") << "\n";` — Print formatted output to the console.
- L125: `        std::cout << "1. CPU Scheduling\n";` — Print formatted output to the console.
- L126: `        std::cout << "2. Memory Management\n";` — Print formatted output to the console.
- L127: `        std::cout << "3. Page Replacement\n";` — Print formatted output to the console.
- L128: `        std::cout << "4. Disk Scheduling\n";` — Print formatted output to the console.
- L129: `        std::cout << "5. Exit\n";` — Print formatted output to the console.
- L130: `` — Blank line for readability and separation.
- L131: `        int choice = ui::readInt("Select option: ", 1, 5);` — Execute this statement as part of the algorithm or UI flow.
- L132: `        if (choice == 5) {` — Conditional check to choose a branch.
- L133: `            std::cout << "\nExiting...\n";` — Print formatted output to the console.
- L134: `            break;` — Execute this statement as part of the algorithm or UI flow.
- L135: `        }` — Close the current scope block.
- L136: `        if (choice == 1) cpuSchedulingMenu();` — Conditional check to choose a branch.
- L137: `        if (choice == 2) memoryManagementMenu();` — Conditional check to choose a branch.
- L138: `        if (choice == 3) pageReplacementMenu();` — Conditional check to choose a branch.
- L139: `        if (choice == 4) diskSchedulingMenu();` — Conditional check to choose a branch.
- L140: `    }` — Close the current scope block.
- L141: `    return 0;` — Return from the function.
- L142: `}` — Close the current scope block.

## File: memory_management\contiguous\best_fit.cpp
- L1: `#include <iomanip>` — Include required standard or project header so symbols compile.
- L2: `#include <iostream>` — Include required standard or project header so symbols compile.
- L3: `#include <string>` — Include required standard or project header so symbols compile.
- L4: `#include <vector>` — Include required standard or project header so symbols compile.
- L5: `` — Blank line for readability and separation.
- L6: `namespace ui {` — Begin namespace scope for UI helpers.
- L7: `    int readInt(const std::string& prompt, int minValue, int maxValue);` — Execute this statement as part of the algorithm or UI flow.
- L8: `    void printSection(const std::string& title);` — Define a function that performs actions without returning a value.
- L9: `}` — Close the current scope block.
- L10: `` — Blank line for readability and separation.
- L11: `static void printAllocationTable(const std::vector<int>& procSize,` — Declare a file-local helper function.
- L12: `                                 const std::vector<int>& blockSize,` — Store sizes of free memory blocks.
- L13: `                                 const std::vector<int>& allocation,` — Store chosen block index for each process.
- L14: `                                 const std::vector<int>& internal) {` — Store internal fragmentation for each allocation.
- L15: `    std::cout << "\n";` — Print formatted output to the console.
- L16: `    std::cout << std::left << std::setw(8) << "PID"` — Print formatted output to the console.
- L17: `              << std::setw(12) << "ProcSize"` — Store memory size required by each process.
- L18: `              << std::setw(10) << "Block"` — Execute this statement as part of the algorithm or UI flow.
- L19: `              << std::setw(12) << "BlockSize"` — Store sizes of free memory blocks.
- L20: `              << std::setw(12) << "IntFrag" << "\n";` — Execute this statement as part of the algorithm or UI flow.
- L21: `    std::cout << std::string(54, '-') << "\n";` — Print formatted output to the console.
- L22: `    for (size_t i = 0; i < procSize.size(); ++i) {` — Store memory size required by each process.
- L23: `        std::cout << std::left << std::setw(8) << (i + 1)` — Print formatted output to the console.
- L24: `                  << std::setw(12) << procSize[i];` — Store memory size required by each process.
- L25: `        if (allocation[i] == -1) {` — Store chosen block index for each process.
- L26: `            std::cout << std::setw(10) << "-"` — Print formatted output to the console.
- L27: `                      << std::setw(12) << "-"` — Execute this statement as part of the algorithm or UI flow.
- L28: `                      << std::setw(12) << "-";` — Execute this statement as part of the algorithm or UI flow.
- L29: `        } else {` — End a scope block.
- L30: `            std::cout << std::setw(10) << (allocation[i] + 1)` — Store chosen block index for each process.
- L31: `                      << std::setw(12) << blockSize[allocation[i]]` — Store sizes of free memory blocks.
- L32: `                      << std::setw(12) << internal[i];` — Store internal fragmentation for each allocation.
- L33: `        }` — Close the current scope block.
- L34: `        std::cout << "\n";` — Print formatted output to the console.
- L35: `    }` — Close the current scope block.
- L36: `}` — Close the current scope block.
- L37: `` — Blank line for readability and separation.
- L38: `void runBestFit() {` — Define a function that performs actions without returning a value.
- L39: `    ui::printSection("Memory Allocation - Best Fit");` — Store chosen block index for each process.
- L40: `    int numBlocks = ui::readInt("Enter number of memory blocks (1-20): ", 1, 20);` — Execute this statement as part of the algorithm or UI flow.
- L41: `    int numProcs = ui::readInt("Enter number of processes (1-20): ", 1, 20);` — Execute this statement as part of the algorithm or UI flow.
- L42: `` — Blank line for readability and separation.
- L43: `    std::vector<int> blockSize(numBlocks);` — Store sizes of free memory blocks.
- L44: `    for (int i = 0; i < numBlocks; ++i) {` — Loop over a range of values.
- L45: `        blockSize[i] = ui::readInt("Block " + std::to_string(i + 1) + " size (1-1024): ", 1, 1024);` — Store sizes of free memory blocks.
- L46: `    }` — Close the current scope block.
- L47: `` — Blank line for readability and separation.
- L48: `    std::vector<int> procSize(numProcs);` — Store memory size required by each process.
- L49: `    for (int i = 0; i < numProcs; ++i) {` — Loop over a range of values.
- L50: `        procSize[i] = ui::readInt("Process " + std::to_string(i + 1) + " size (1-1024): ", 1, 1024);` — Store memory size required by each process.
- L51: `    }` — Close the current scope block.
- L52: `` — Blank line for readability and separation.
- L53: `    // allocation[i] holds the chosen block index for process i, or -1 if not allocated.` — Comment added to explain intent or algorithm detail.
- L54: `    std::vector<int> allocation(numProcs, -1);` — Store chosen block index for each process.
- L55: `    std::vector<int> internal(numProcs, 0);` — Store internal fragmentation for each allocation.
- L56: `    std::vector<bool> used(numBlocks, false);` — Track whether a block is already allocated.
- L57: `` — Blank line for readability and separation.
- L58: `    for (int i = 0; i < numProcs; ++i) {` — Loop over a range of values.
- L59: `        int chosen = -1;` — Execute this statement as part of the algorithm or UI flow.
- L60: `        int bestSize = 1e9;` — Execute this statement as part of the algorithm or UI flow.
- L61: `        for (int b = 0; b < numBlocks; ++b) {` — Loop over a range of values.
- L62: `            // Best Fit picks the smallest block that still fits.` — Comment added to explain intent or algorithm detail.
- L63: `            if (!used[b] && blockSize[b] >= procSize[i] && blockSize[b] < bestSize) {` — Store sizes of free memory blocks.
- L64: `                bestSize = blockSize[b];` — Store sizes of free memory blocks.
- L65: `                chosen = b;` — Execute this statement as part of the algorithm or UI flow.
- L66: `            }` — Close the current scope block.
- L67: `        }` — Close the current scope block.
- L68: `` — Blank line for readability and separation.
- L69: `        if (chosen != -1) {` — Conditional check to choose a branch.
- L70: `            allocation[i] = chosen;` — Store chosen block index for each process.
- L71: `            used[chosen] = true;` — Track whether a block is already allocated.
- L72: `            // Internal fragmentation is the unused space inside the allocated block.` — Comment added to explain intent or algorithm detail.
- L73: `            internal[i] = blockSize[chosen] - procSize[i];` — Store sizes of free memory blocks.
- L74: `        }` — Close the current scope block.
- L75: `    }` — Close the current scope block.
- L76: `` — Blank line for readability and separation.
- L77: `    printAllocationTable(procSize, blockSize, allocation, internal);` — Store sizes of free memory blocks.
- L78: `` — Blank line for readability and separation.
- L79: `    int totalInternal = 0;` — Store internal fragmentation for each allocation.
- L80: `    int totalExternal = 0;` — Execute this statement as part of the algorithm or UI flow.
- L81: `    int unallocated = 0;` — Execute this statement as part of the algorithm or UI flow.
- L82: `    for (int i = 0; i < numProcs; ++i) {` — Loop over a range of values.
- L83: `        if (allocation[i] == -1) unallocated++;` — Store chosen block index for each process.
- L84: `        totalInternal += internal[i];` — Store internal fragmentation for each allocation.
- L85: `    }` — Close the current scope block.
- L86: `    for (int b = 0; b < numBlocks; ++b) {` — Loop over a range of values.
- L87: `        // External fragmentation is the sum of free blocks left unused.` — Comment added to explain intent or algorithm detail.
- L88: `        if (!used[b]) totalExternal += blockSize[b];` — Store sizes of free memory blocks.
- L89: `    }` — Close the current scope block.
- L90: `` — Blank line for readability and separation.
- L91: `    std::cout << "\nTotal Internal Fragmentation: " << totalInternal << "\n";` — Store internal fragmentation for each allocation.
- L92: `    std::cout << "Total External Fragmentation: " << totalExternal << "\n";` — Print formatted output to the console.
- L93: `    std::cout << "Unallocated Processes: " << unallocated << "\n";` — Print formatted output to the console.
- L94: `}` — Close the current scope block.

## File: memory_management\contiguous\first_fit.cpp
- L1: `#include <iomanip>` — Include required standard or project header so symbols compile.
- L2: `#include <iostream>` — Include required standard or project header so symbols compile.
- L3: `#include <string>` — Include required standard or project header so symbols compile.
- L4: `#include <vector>` — Include required standard or project header so symbols compile.
- L5: `` — Blank line for readability and separation.
- L6: `namespace ui {` — Begin namespace scope for UI helpers.
- L7: `    int readInt(const std::string& prompt, int minValue, int maxValue);` — Execute this statement as part of the algorithm or UI flow.
- L8: `    void printSection(const std::string& title);` — Define a function that performs actions without returning a value.
- L9: `}` — Close the current scope block.
- L10: `` — Blank line for readability and separation.
- L11: `static void printAllocationTable(const std::vector<int>& procSize,` — Declare a file-local helper function.
- L12: `                                 const std::vector<int>& blockSize,` — Store sizes of free memory blocks.
- L13: `                                 const std::vector<int>& allocation,` — Store chosen block index for each process.
- L14: `                                 const std::vector<int>& internal) {` — Store internal fragmentation for each allocation.
- L15: `    std::cout << "\n";` — Print formatted output to the console.
- L16: `    std::cout << std::left << std::setw(8) << "PID"` — Print formatted output to the console.
- L17: `              << std::setw(12) << "ProcSize"` — Store memory size required by each process.
- L18: `              << std::setw(10) << "Block"` — Execute this statement as part of the algorithm or UI flow.
- L19: `              << std::setw(12) << "BlockSize"` — Store sizes of free memory blocks.
- L20: `              << std::setw(12) << "IntFrag" << "\n";` — Execute this statement as part of the algorithm or UI flow.
- L21: `    std::cout << std::string(54, '-') << "\n";` — Print formatted output to the console.
- L22: `    for (size_t i = 0; i < procSize.size(); ++i) {` — Store memory size required by each process.
- L23: `        std::cout << std::left << std::setw(8) << (i + 1)` — Print formatted output to the console.
- L24: `                  << std::setw(12) << procSize[i];` — Store memory size required by each process.
- L25: `        if (allocation[i] == -1) {` — Store chosen block index for each process.
- L26: `            std::cout << std::setw(10) << "-"` — Print formatted output to the console.
- L27: `                      << std::setw(12) << "-"` — Execute this statement as part of the algorithm or UI flow.
- L28: `                      << std::setw(12) << "-";` — Execute this statement as part of the algorithm or UI flow.
- L29: `        } else {` — End a scope block.
- L30: `            std::cout << std::setw(10) << (allocation[i] + 1)` — Store chosen block index for each process.
- L31: `                      << std::setw(12) << blockSize[allocation[i]]` — Store sizes of free memory blocks.
- L32: `                      << std::setw(12) << internal[i];` — Store internal fragmentation for each allocation.
- L33: `        }` — Close the current scope block.
- L34: `        std::cout << "\n";` — Print formatted output to the console.
- L35: `    }` — Close the current scope block.
- L36: `}` — Close the current scope block.
- L37: `` — Blank line for readability and separation.
- L38: `void runFirstFit() {` — Define a function that performs actions without returning a value.
- L39: `    ui::printSection("Memory Allocation - First Fit");` — Store chosen block index for each process.
- L40: `    int numBlocks = ui::readInt("Enter number of memory blocks (1-20): ", 1, 20);` — Execute this statement as part of the algorithm or UI flow.
- L41: `    int numProcs = ui::readInt("Enter number of processes (1-20): ", 1, 20);` — Execute this statement as part of the algorithm or UI flow.
- L42: `` — Blank line for readability and separation.
- L43: `    std::vector<int> blockSize(numBlocks);` — Store sizes of free memory blocks.
- L44: `    for (int i = 0; i < numBlocks; ++i) {` — Loop over a range of values.
- L45: `        blockSize[i] = ui::readInt("Block " + std::to_string(i + 1) + " size (1-1024): ", 1, 1024);` — Store sizes of free memory blocks.
- L46: `    }` — Close the current scope block.
- L47: `` — Blank line for readability and separation.
- L48: `    std::vector<int> procSize(numProcs);` — Store memory size required by each process.
- L49: `    for (int i = 0; i < numProcs; ++i) {` — Loop over a range of values.
- L50: `        procSize[i] = ui::readInt("Process " + std::to_string(i + 1) + " size (1-1024): ", 1, 1024);` — Store memory size required by each process.
- L51: `    }` — Close the current scope block.
- L52: `` — Blank line for readability and separation.
- L53: `    // allocation[i] holds the chosen block index for process i, or -1 if not allocated.` — Comment added to explain intent or algorithm detail.
- L54: `    std::vector<int> allocation(numProcs, -1);` — Store chosen block index for each process.
- L55: `    std::vector<int> internal(numProcs, 0);` — Store internal fragmentation for each allocation.
- L56: `    std::vector<bool> used(numBlocks, false);` — Track whether a block is already allocated.
- L57: `` — Blank line for readability and separation.
- L58: `    for (int i = 0; i < numProcs; ++i) {` — Loop over a range of values.
- L59: `        int chosen = -1;` — Execute this statement as part of the algorithm or UI flow.
- L60: `        for (int b = 0; b < numBlocks; ++b) {` — Loop over a range of values.
- L61: `            // First Fit picks the first block that can hold the process.` — Comment added to explain intent or algorithm detail.
- L62: `            if (!used[b] && blockSize[b] >= procSize[i]) {` — Store sizes of free memory blocks.
- L63: `                chosen = b;` — Execute this statement as part of the algorithm or UI flow.
- L64: `                break;` — Execute this statement as part of the algorithm or UI flow.
- L65: `            }` — Close the current scope block.
- L66: `        }` — Close the current scope block.
- L67: `` — Blank line for readability and separation.
- L68: `        if (chosen != -1) {` — Conditional check to choose a branch.
- L69: `            allocation[i] = chosen;` — Store chosen block index for each process.
- L70: `            used[chosen] = true;` — Track whether a block is already allocated.
- L71: `            // Internal fragmentation is the unused space inside the allocated block.` — Comment added to explain intent or algorithm detail.
- L72: `            internal[i] = blockSize[chosen] - procSize[i];` — Store sizes of free memory blocks.
- L73: `        }` — Close the current scope block.
- L74: `    }` — Close the current scope block.
- L75: `` — Blank line for readability and separation.
- L76: `    printAllocationTable(procSize, blockSize, allocation, internal);` — Store sizes of free memory blocks.
- L77: `` — Blank line for readability and separation.
- L78: `    int totalInternal = 0;` — Store internal fragmentation for each allocation.
- L79: `    int totalExternal = 0;` — Execute this statement as part of the algorithm or UI flow.
- L80: `    int unallocated = 0;` — Execute this statement as part of the algorithm or UI flow.
- L81: `    for (int i = 0; i < numProcs; ++i) {` — Loop over a range of values.
- L82: `        if (allocation[i] == -1) unallocated++;` — Store chosen block index for each process.
- L83: `        totalInternal += internal[i];` — Store internal fragmentation for each allocation.
- L84: `    }` — Close the current scope block.
- L85: `    for (int b = 0; b < numBlocks; ++b) {` — Loop over a range of values.
- L86: `        // External fragmentation is the sum of free blocks left unused.` — Comment added to explain intent or algorithm detail.
- L87: `        if (!used[b]) totalExternal += blockSize[b];` — Store sizes of free memory blocks.
- L88: `    }` — Close the current scope block.
- L89: `` — Blank line for readability and separation.
- L90: `    std::cout << "\nTotal Internal Fragmentation: " << totalInternal << "\n";` — Store internal fragmentation for each allocation.
- L91: `    std::cout << "Total External Fragmentation: " << totalExternal << "\n";` — Print formatted output to the console.
- L92: `    std::cout << "Unallocated Processes: " << unallocated << "\n";` — Print formatted output to the console.
- L93: `}` — Close the current scope block.

## File: memory_management\contiguous\next_fit.cpp
- L1: `#include <iomanip>` — Include required standard or project header so symbols compile.
- L2: `#include <iostream>` — Include required standard or project header so symbols compile.
- L3: `#include <string>` — Include required standard or project header so symbols compile.
- L4: `#include <vector>` — Include required standard or project header so symbols compile.
- L5: `` — Blank line for readability and separation.
- L6: `namespace ui {` — Begin namespace scope for UI helpers.
- L7: `    int readInt(const std::string& prompt, int minValue, int maxValue);` — Execute this statement as part of the algorithm or UI flow.
- L8: `    void printSection(const std::string& title);` — Define a function that performs actions without returning a value.
- L9: `}` — Close the current scope block.
- L10: `` — Blank line for readability and separation.
- L11: `static void printAllocationTable(const std::vector<int>& procSize,` — Declare a file-local helper function.
- L12: `                                 const std::vector<int>& blockSize,` — Store sizes of free memory blocks.
- L13: `                                 const std::vector<int>& allocation,` — Store chosen block index for each process.
- L14: `                                 const std::vector<int>& internal) {` — Store internal fragmentation for each allocation.
- L15: `    std::cout << "\n";` — Print formatted output to the console.
- L16: `    std::cout << std::left << std::setw(8) << "PID"` — Print formatted output to the console.
- L17: `              << std::setw(12) << "ProcSize"` — Store memory size required by each process.
- L18: `              << std::setw(10) << "Block"` — Execute this statement as part of the algorithm or UI flow.
- L19: `              << std::setw(12) << "BlockSize"` — Store sizes of free memory blocks.
- L20: `              << std::setw(12) << "IntFrag" << "\n";` — Execute this statement as part of the algorithm or UI flow.
- L21: `    std::cout << std::string(54, '-') << "\n";` — Print formatted output to the console.
- L22: `    for (size_t i = 0; i < procSize.size(); ++i) {` — Store memory size required by each process.
- L23: `        std::cout << std::left << std::setw(8) << (i + 1)` — Print formatted output to the console.
- L24: `                  << std::setw(12) << procSize[i];` — Store memory size required by each process.
- L25: `        if (allocation[i] == -1) {` — Store chosen block index for each process.
- L26: `            std::cout << std::setw(10) << "-"` — Print formatted output to the console.
- L27: `                      << std::setw(12) << "-"` — Execute this statement as part of the algorithm or UI flow.
- L28: `                      << std::setw(12) << "-";` — Execute this statement as part of the algorithm or UI flow.
- L29: `        } else {` — End a scope block.
- L30: `            std::cout << std::setw(10) << (allocation[i] + 1)` — Store chosen block index for each process.
- L31: `                      << std::setw(12) << blockSize[allocation[i]]` — Store sizes of free memory blocks.
- L32: `                      << std::setw(12) << internal[i];` — Store internal fragmentation for each allocation.
- L33: `        }` — Close the current scope block.
- L34: `        std::cout << "\n";` — Print formatted output to the console.
- L35: `    }` — Close the current scope block.
- L36: `}` — Close the current scope block.
- L37: `` — Blank line for readability and separation.
- L38: `void runNextFit() {` — Define a function that performs actions without returning a value.
- L39: `    ui::printSection("Memory Allocation - Next Fit");` — Store chosen block index for each process.
- L40: `    int numBlocks = ui::readInt("Enter number of memory blocks (1-20): ", 1, 20);` — Execute this statement as part of the algorithm or UI flow.
- L41: `    int numProcs = ui::readInt("Enter number of processes (1-20): ", 1, 20);` — Execute this statement as part of the algorithm or UI flow.
- L42: `` — Blank line for readability and separation.
- L43: `    std::vector<int> blockSize(numBlocks);` — Store sizes of free memory blocks.
- L44: `    for (int i = 0; i < numBlocks; ++i) {` — Loop over a range of values.
- L45: `        blockSize[i] = ui::readInt("Block " + std::to_string(i + 1) + " size (1-1024): ", 1, 1024);` — Store sizes of free memory blocks.
- L46: `    }` — Close the current scope block.
- L47: `` — Blank line for readability and separation.
- L48: `    std::vector<int> procSize(numProcs);` — Store memory size required by each process.
- L49: `    for (int i = 0; i < numProcs; ++i) {` — Loop over a range of values.
- L50: `        procSize[i] = ui::readInt("Process " + std::to_string(i + 1) + " size (1-1024): ", 1, 1024);` — Store memory size required by each process.
- L51: `    }` — Close the current scope block.
- L52: `` — Blank line for readability and separation.
- L53: `    // allocation[i] holds the chosen block index for process i, or -1 if not allocated.` — Comment added to explain intent or algorithm detail.
- L54: `    std::vector<int> allocation(numProcs, -1);` — Store chosen block index for each process.
- L55: `    std::vector<int> internal(numProcs, 0);` — Store internal fragmentation for each allocation.
- L56: `    std::vector<bool> used(numBlocks, false);` — Track whether a block is already allocated.
- L57: `    int nextIndex = 0; // Starting point for Next Fit search.` — Execute this statement as part of the algorithm or UI flow.
- L58: `` — Blank line for readability and separation.
- L59: `    for (int i = 0; i < numProcs; ++i) {` — Loop over a range of values.
- L60: `        int chosen = -1;` — Execute this statement as part of the algorithm or UI flow.
- L61: `        int count = 0;` — Execute this statement as part of the algorithm or UI flow.
- L62: `        while (count < numBlocks) {` — Loop while a condition remains true.
- L63: `            // Next Fit continues from the last allocated block.` — Comment added to explain intent or algorithm detail.
- L64: `            if (!used[nextIndex] && blockSize[nextIndex] >= procSize[i]) {` — Store sizes of free memory blocks.
- L65: `                chosen = nextIndex;` — Execute this statement as part of the algorithm or UI flow.
- L66: `                break;` — Execute this statement as part of the algorithm or UI flow.
- L67: `            }` — Close the current scope block.
- L68: `            nextIndex = (nextIndex + 1) % numBlocks;` — Execute this statement as part of the algorithm or UI flow.
- L69: `            count++;` — Execute this statement as part of the algorithm or UI flow.
- L70: `        }` — Close the current scope block.
- L71: `` — Blank line for readability and separation.
- L72: `        if (chosen != -1) {` — Conditional check to choose a branch.
- L73: `            allocation[i] = chosen;` — Store chosen block index for each process.
- L74: `            used[chosen] = true;` — Track whether a block is already allocated.
- L75: `            // Internal fragmentation is the unused space inside the allocated block.` — Comment added to explain intent or algorithm detail.
- L76: `            internal[i] = blockSize[chosen] - procSize[i];` — Store sizes of free memory blocks.
- L77: `            nextIndex = (chosen + 1) % numBlocks;` — Execute this statement as part of the algorithm or UI flow.
- L78: `        }` — Close the current scope block.
- L79: `    }` — Close the current scope block.
- L80: `` — Blank line for readability and separation.
- L81: `    printAllocationTable(procSize, blockSize, allocation, internal);` — Store sizes of free memory blocks.
- L82: `` — Blank line for readability and separation.
- L83: `    int totalInternal = 0;` — Store internal fragmentation for each allocation.
- L84: `    int totalExternal = 0;` — Execute this statement as part of the algorithm or UI flow.
- L85: `    int unallocated = 0;` — Execute this statement as part of the algorithm or UI flow.
- L86: `    for (int i = 0; i < numProcs; ++i) {` — Loop over a range of values.
- L87: `        if (allocation[i] == -1) unallocated++;` — Store chosen block index for each process.
- L88: `        totalInternal += internal[i];` — Store internal fragmentation for each allocation.
- L89: `    }` — Close the current scope block.
- L90: `    for (int b = 0; b < numBlocks; ++b) {` — Loop over a range of values.
- L91: `        // External fragmentation is the sum of free blocks left unused.` — Comment added to explain intent or algorithm detail.
- L92: `        if (!used[b]) totalExternal += blockSize[b];` — Store sizes of free memory blocks.
- L93: `    }` — Close the current scope block.
- L94: `` — Blank line for readability and separation.
- L95: `    std::cout << "\nTotal Internal Fragmentation: " << totalInternal << "\n";` — Store internal fragmentation for each allocation.
- L96: `    std::cout << "Total External Fragmentation: " << totalExternal << "\n";` — Print formatted output to the console.
- L97: `    std::cout << "Unallocated Processes: " << unallocated << "\n";` — Print formatted output to the console.
- L98: `}` — Close the current scope block.

## File: page_replacement\fifo.cpp
- L1: `#include <algorithm>` — Include required standard or project header so symbols compile.
- L2: `#include <iomanip>` — Include required standard or project header so symbols compile.
- L3: `#include <iostream>` — Include required standard or project header so symbols compile.
- L4: `#include <string>` — Include required standard or project header so symbols compile.
- L5: `#include <queue>` — Include required standard or project header so symbols compile.
- L6: `#include <vector>` — Include required standard or project header so symbols compile.
- L7: `` — Blank line for readability and separation.
- L8: `namespace ui {` — Begin namespace scope for UI helpers.
- L9: `    int readInt(const std::string& prompt, int minValue, int maxValue);` — Execute this statement as part of the algorithm or UI flow.
- L10: `    void printSection(const std::string& title);` — Define a function that performs actions without returning a value.
- L11: `}` — Close the current scope block.
- L12: `` — Blank line for readability and separation.
- L13: `static void printFrames(const std::vector<int>& frames) {` — Declare a file-local helper function.
- L14: `    std::cout << "Frames: ";` — Print formatted output to the console.
- L15: `    for (int f : frames) {` — Loop over a range of values.
- L16: `        if (f == -1) std::cout << "- ";` — Conditional check to choose a branch.
- L17: `        else std::cout << f << " ";` — Fallback branch for previous condition.
- L18: `    }` — Close the current scope block.
- L19: `    std::cout << "\n";` — Print formatted output to the console.
- L20: `}` — Close the current scope block.
- L21: `` — Blank line for readability and separation.
- L22: `void runFIFOPageReplacement() {` — Define a function that performs actions without returning a value.
- L23: `    ui::printSection("Page Replacement - FIFO");` — Execute this statement as part of the algorithm or UI flow.
- L24: `    int framesCount = ui::readInt("Enter number of frames (1-10): ", 1, 10);` — Execute this statement as part of the algorithm or UI flow.
- L25: `    int n = ui::readInt("Enter reference string length (1-20): ", 1, 20);` — Execute this statement as part of the algorithm or UI flow.
- L26: `` — Blank line for readability and separation.
- L27: `    std::vector<int> refs(n);` — Execute this statement as part of the algorithm or UI flow.
- L28: `    for (int i = 0; i < n; ++i) {` — Loop over a range of values.
- L29: `        refs[i] = ui::readInt("Reference " + std::to_string(i + 1) + " (0-9): ", 0, 9);` — Execute this statement as part of the algorithm or UI flow.
- L30: `    }` — Close the current scope block.
- L31: `` — Blank line for readability and separation.
- L32: `    std::vector<int> frames(framesCount, -1);` — Execute this statement as part of the algorithm or UI flow.
- L33: `    std::queue<int> q; // Stores frame indices in arrival order for FIFO.` — FIFO queue keeps the oldest loaded frame for replacement.
- L34: `    int faults = 0;` — Count page faults for performance metric.
- L35: `` — Blank line for readability and separation.
- L36: `    for (int i = 0; i < n; ++i) {` — Loop over a range of values.
- L37: `        int page = refs[i];` — Execute this statement as part of the algorithm or UI flow.
- L38: `        // HIT means page already loaded in any frame.` — Comment added to explain intent or algorithm detail.
- L39: `        bool hit = std::find(frames.begin(), frames.end(), page) != frames.end();` — Determine whether the referenced page is already in memory.
- L40: `        std::cout << "\nStep " << (i + 1) << ": Page " << page << (hit ? " (HIT)" : " (FAULT)") << "\n";` — Determine whether the referenced page is already in memory.
- L41: `` — Blank line for readability and separation.
- L42: `        if (!hit) {` — Determine whether the referenced page is already in memory.
- L43: `            faults++;` — Count page faults for performance metric.
- L44: `            if (static_cast<int>(q.size()) < framesCount) {` — Conditional check to choose a branch.
- L45: `                // Fill empty frame first.` — Comment added to explain intent or algorithm detail.
- L46: `                for (int f = 0; f < framesCount; ++f) {` — Loop over a range of values.
- L47: `                    if (frames[f] == -1) {` — Conditional check to choose a branch.
- L48: `                        frames[f] = page;` — Execute this statement as part of the algorithm or UI flow.
- L49: `                        q.push(f);` — Execute this statement as part of the algorithm or UI flow.
- L50: `                        break;` — Execute this statement as part of the algorithm or UI flow.
- L51: `                    }` — Close the current scope block.
- L52: `                }` — Close the current scope block.
- L53: `            } else {` — End a scope block.
- L54: `                // Evict the oldest loaded frame.` — Comment added to explain intent or algorithm detail.
- L55: `                int idx = q.front();` — Execute this statement as part of the algorithm or UI flow.
- L56: `                q.pop();` — Execute this statement as part of the algorithm or UI flow.
- L57: `                frames[idx] = page;` — Execute this statement as part of the algorithm or UI flow.
- L58: `                q.push(idx);` — Execute this statement as part of the algorithm or UI flow.
- L59: `            }` — Close the current scope block.
- L60: `        }` — Close the current scope block.
- L61: `        printFrames(frames);` — Execute this statement as part of the algorithm or UI flow.
- L62: `    }` — Close the current scope block.
- L63: `` — Blank line for readability and separation.
- L64: `    std::cout << "\nTotal Page Faults: " << faults << "\n";` — Count page faults for performance metric.
- L65: `}` — Close the current scope block.

## File: page_replacement\lru.cpp
- L1: `#include <algorithm>` — Include required standard or project header so symbols compile.
- L2: `#include <iostream>` — Include required standard or project header so symbols compile.
- L3: `#include <string>` — Include required standard or project header so symbols compile.
- L4: `#include <vector>` — Include required standard or project header so symbols compile.
- L5: `` — Blank line for readability and separation.
- L6: `namespace ui {` — Begin namespace scope for UI helpers.
- L7: `    int readInt(const std::string& prompt, int minValue, int maxValue);` — Execute this statement as part of the algorithm or UI flow.
- L8: `    void printSection(const std::string& title);` — Define a function that performs actions without returning a value.
- L9: `}` — Close the current scope block.
- L10: `` — Blank line for readability and separation.
- L11: `static void printFrames(const std::vector<int>& frames) {` — Declare a file-local helper function.
- L12: `    std::cout << "Frames: ";` — Print formatted output to the console.
- L13: `    for (int f : frames) {` — Loop over a range of values.
- L14: `        if (f == -1) std::cout << "- ";` — Conditional check to choose a branch.
- L15: `        else std::cout << f << " ";` — Fallback branch for previous condition.
- L16: `    }` — Close the current scope block.
- L17: `    std::cout << "\n";` — Print formatted output to the console.
- L18: `}` — Close the current scope block.
- L19: `` — Blank line for readability and separation.
- L20: `void runLRUPageReplacement() {` — Define a function that performs actions without returning a value.
- L21: `    ui::printSection("Page Replacement - LRU");` — Execute this statement as part of the algorithm or UI flow.
- L22: `    int framesCount = ui::readInt("Enter number of frames (1-10): ", 1, 10);` — Execute this statement as part of the algorithm or UI flow.
- L23: `    int n = ui::readInt("Enter reference string length (1-20): ", 1, 20);` — Execute this statement as part of the algorithm or UI flow.
- L24: `` — Blank line for readability and separation.
- L25: `    std::vector<int> refs(n);` — Execute this statement as part of the algorithm or UI flow.
- L26: `    for (int i = 0; i < n; ++i) {` — Loop over a range of values.
- L27: `        refs[i] = ui::readInt("Reference " + std::to_string(i + 1) + " (0-9): ", 0, 9);` — Execute this statement as part of the algorithm or UI flow.
- L28: `    }` — Close the current scope block.
- L29: `` — Blank line for readability and separation.
- L30: `    std::vector<int> frames(framesCount, -1);` — Execute this statement as part of the algorithm or UI flow.
- L31: `    std::vector<int> lastUsed(framesCount, -1); // Stores last access time per frame.` — Track last access index for LRU replacement.
- L32: `    int faults = 0;` — Execute this statement as part of the algorithm or UI flow.
- L33: `` — Blank line for readability and separation.
- L34: `    for (int i = 0; i < n; ++i) {` — Loop over a range of values.
- L35: `        int page = refs[i];` — Execute this statement as part of the algorithm or UI flow.
- L36: `        bool hit = false;` — Execute this statement as part of the algorithm or UI flow.
- L37: `        for (int f = 0; f < framesCount; ++f) {` — Loop over a range of values.
- L38: `            if (frames[f] == page) {` — Conditional check to choose a branch.
- L39: `                // Update last used time on hit.` — Comment added to explain intent or algorithm detail.
- L40: `                lastUsed[f] = i;` — Track last access index for LRU replacement.
- L41: `                hit = true;` — Execute this statement as part of the algorithm or UI flow.
- L42: `                break;` — Execute this statement as part of the algorithm or UI flow.
- L43: `            }` — Close the current scope block.
- L44: `        }` — Close the current scope block.
- L45: `` — Blank line for readability and separation.
- L46: `        std::cout << "\nStep " << (i + 1) << ": Page " << page << (hit ? " (HIT)" : " (FAULT)") << "\n";` — Print formatted output to the console.
- L47: `        if (!hit) {` — Conditional check to choose a branch.
- L48: `            faults++;` — Execute this statement as part of the algorithm or UI flow.
- L49: `            int empty = -1;` — Execute this statement as part of the algorithm or UI flow.
- L50: `            for (int f = 0; f < framesCount; ++f) {` — Loop over a range of values.
- L51: `                if (frames[f] == -1) { empty = f; break; }` — Conditional check to choose a branch.
- L52: `            }` — Close the current scope block.
- L53: `            if (empty != -1) {` — Conditional check to choose a branch.
- L54: `                frames[empty] = page;` — Execute this statement as part of the algorithm or UI flow.
- L55: `                lastUsed[empty] = i;` — Track last access index for LRU replacement.
- L56: `            } else {` — End a scope block.
- L57: `                // Evict the least recently used frame.` — Comment added to explain intent or algorithm detail.
- L58: `                int lruIndex = 0;` — Select the least recently used frame to evict.
- L59: `                for (int f = 1; f < framesCount; ++f) {` — Loop over a range of values.
- L60: `                    if (lastUsed[f] < lastUsed[lruIndex]) lruIndex = f;` — Track last access index for LRU replacement.
- L61: `                }` — Close the current scope block.
- L62: `                frames[lruIndex] = page;` — Select the least recently used frame to evict.
- L63: `                lastUsed[lruIndex] = i;` — Track last access index for LRU replacement.
- L64: `            }` — Close the current scope block.
- L65: `        }` — Close the current scope block.
- L66: `        printFrames(frames);` — Execute this statement as part of the algorithm or UI flow.
- L67: `    }` — Close the current scope block.
- L68: `` — Blank line for readability and separation.
- L69: `    std::cout << "\nTotal Page Faults: " << faults << "\n";` — Print formatted output to the console.
- L70: `}` — Close the current scope block.

## File: page_replacement\optimal.cpp
- L1: `#include <algorithm>` — Include required standard or project header so symbols compile.
- L2: `#include <iostream>` — Include required standard or project header so symbols compile.
- L3: `#include <string>` — Include required standard or project header so symbols compile.
- L4: `#include <vector>` — Include required standard or project header so symbols compile.
- L5: `` — Blank line for readability and separation.
- L6: `namespace ui {` — Begin namespace scope for UI helpers.
- L7: `    int readInt(const std::string& prompt, int minValue, int maxValue);` — Execute this statement as part of the algorithm or UI flow.
- L8: `    void printSection(const std::string& title);` — Define a function that performs actions without returning a value.
- L9: `}` — Close the current scope block.
- L10: `` — Blank line for readability and separation.
- L11: `static void printFrames(const std::vector<int>& frames) {` — Declare a file-local helper function.
- L12: `    std::cout << "Frames: ";` — Print formatted output to the console.
- L13: `    for (int f : frames) {` — Loop over a range of values.
- L14: `        if (f == -1) std::cout << "- ";` — Conditional check to choose a branch.
- L15: `        else std::cout << f << " ";` — Fallback branch for previous condition.
- L16: `    }` — Close the current scope block.
- L17: `    std::cout << "\n";` — Print formatted output to the console.
- L18: `}` — Close the current scope block.
- L19: `` — Blank line for readability and separation.
- L20: `void runOptimalPageReplacement() {` — Define a function that performs actions without returning a value.
- L21: `    ui::printSection("Page Replacement - Optimal");` — Execute this statement as part of the algorithm or UI flow.
- L22: `    int framesCount = ui::readInt("Enter number of frames (1-10): ", 1, 10);` — Execute this statement as part of the algorithm or UI flow.
- L23: `    int n = ui::readInt("Enter reference string length (1-20): ", 1, 20);` — Execute this statement as part of the algorithm or UI flow.
- L24: `` — Blank line for readability and separation.
- L25: `    std::vector<int> refs(n);` — Execute this statement as part of the algorithm or UI flow.
- L26: `    for (int i = 0; i < n; ++i) {` — Loop over a range of values.
- L27: `        refs[i] = ui::readInt("Reference " + std::to_string(i + 1) + " (0-9): ", 0, 9);` — Execute this statement as part of the algorithm or UI flow.
- L28: `    }` — Close the current scope block.
- L29: `` — Blank line for readability and separation.
- L30: `    std::vector<int> frames(framesCount, -1);` — Execute this statement as part of the algorithm or UI flow.
- L31: `    int faults = 0;` — Execute this statement as part of the algorithm or UI flow.
- L32: `` — Blank line for readability and separation.
- L33: `    for (int i = 0; i < n; ++i) {` — Loop over a range of values.
- L34: `        int page = refs[i];` — Execute this statement as part of the algorithm or UI flow.
- L35: `        // HIT means page already loaded in any frame.` — Comment added to explain intent or algorithm detail.
- L36: `        bool hit = std::find(frames.begin(), frames.end(), page) != frames.end();` — Execute this statement as part of the algorithm or UI flow.
- L37: `        std::cout << "\nStep " << (i + 1) << ": Page " << page << (hit ? " (HIT)" : " (FAULT)") << "\n";` — Print formatted output to the console.
- L38: `` — Blank line for readability and separation.
- L39: `        if (!hit) {` — Conditional check to choose a branch.
- L40: `            faults++;` — Execute this statement as part of the algorithm or UI flow.
- L41: `            int empty = -1;` — Execute this statement as part of the algorithm or UI flow.
- L42: `            for (int f = 0; f < framesCount; ++f) {` — Loop over a range of values.
- L43: `                if (frames[f] == -1) { empty = f; break; }` — Conditional check to choose a branch.
- L44: `            }` — Close the current scope block.
- L45: `            if (empty != -1) {` — Conditional check to choose a branch.
- L46: `                frames[empty] = page;` — Execute this statement as part of the algorithm or UI flow.
- L47: `            } else {` — End a scope block.
- L48: `                // Optimal replacement evicts the page used farthest in the future.` — Comment added to explain intent or algorithm detail.
- L49: `                int victim = -1;` — Execute this statement as part of the algorithm or UI flow.
- L50: `                int farthest = -1;` — Track farthest future use for optimal replacement.
- L51: `                for (int f = 0; f < framesCount; ++f) {` — Loop over a range of values.
- L52: `                    int nextUse = 1e9;` — Find next future access to decide optimal victim.
- L53: `                    for (int j = i + 1; j < n; ++j) {` — Loop over a range of values.
- L54: `                        if (refs[j] == frames[f]) { nextUse = j; break; }` — Find next future access to decide optimal victim.
- L55: `                    }` — Close the current scope block.
- L56: `                    if (nextUse > farthest) {` — Track farthest future use for optimal replacement.
- L57: `                        farthest = nextUse;` — Track farthest future use for optimal replacement.
- L58: `                        victim = f;` — Execute this statement as part of the algorithm or UI flow.
- L59: `                    }` — Close the current scope block.
- L60: `                }` — Close the current scope block.
- L61: `                frames[victim] = page;` — Execute this statement as part of the algorithm or UI flow.
- L62: `            }` — Close the current scope block.
- L63: `        }` — Close the current scope block.
- L64: `        printFrames(frames);` — Execute this statement as part of the algorithm or UI flow.
- L65: `    }` — Close the current scope block.
- L66: `` — Blank line for readability and separation.
- L67: `    std::cout << "\nTotal Page Faults: " << faults << "\n";` — Print formatted output to the console.
- L68: `}` — Close the current scope block.

## File: README.md
- L1: `# Mini Operating System Simulator (Console)` — Markdown heading that organizes the document.
- L2: `` — Blank line for readability and separation.
- L3: `## Project Description` — Markdown heading that organizes the document.
- L4: `MiniOS-Simulator is a menu-driven C++ console application that simulates major Operating System algorithms. It includes CPU scheduling, memory management, page replacement, and disk scheduling algorithms with step-by-step output and formatted tables.` — Markdown paragraph text for documentation.
- L5: `` — Blank line for readability and separation.
- L6: `Developer: SABYASACHHI ROUTRAY` — Markdown paragraph text for documentation.
- L7: `` — Blank line for readability and separation.
- L8: `## Algorithms Included` — Markdown heading that organizes the document.
- L9: `` — Blank line for readability and separation.
- L10: `### CPU Scheduling` — Markdown heading that organizes the document.
- L11: `- FCFS (First-Come, First-Served)` — Markdown list item that enumerates content.
- L12: `- SJF (Non-Preemptive)` — Markdown list item that enumerates content.
- L13: `- SJF (Preemptive / SRTF)` — Markdown list item that enumerates content.
- L14: `- Priority Scheduling (Non-Preemptive)` — Markdown list item that enumerates content.
- L15: `- Round Robin` — Markdown list item that enumerates content.
- L16: `` — Blank line for readability and separation.
- L17: `### Memory Management` — Markdown heading that organizes the document.
- L18: `- Contiguous Allocation: First Fit, Best Fit, Next Fit` — Markdown list item that enumerates content.
- L19: `- Non-Contiguous Allocation: Paging Allocation` — Markdown list item that enumerates content.
- L20: `` — Blank line for readability and separation.
- L21: `` — Blank line for readability and separation.
- L22: `### Page Replacement` — Markdown heading that organizes the document.
- L23: `- FIFO` — Markdown list item that enumerates content.
- L24: `- LRU` — Markdown list item that enumerates content.
- L25: `- Optimal` — Markdown list item that enumerates content.
- L26: `` — Blank line for readability and separation.
- L27: `### Disk Scheduling` — Markdown heading that organizes the document.
- L28: `- FCFS` — Markdown list item that enumerates content.
- L29: `- SSTF` — Markdown list item that enumerates content.
- L30: `- SCAN` — Markdown list item that enumerates content.
- L31: `- C-SCAN` — Markdown list item that enumerates content.
- L32: `` — Blank line for readability and separation.
- L33: `## Compilation Steps` — Markdown heading that organizes the document.
- L34: `From the project root:` — Markdown paragraph text for documentation.
- L35: `` — Blank line for readability and separation.
- L36: ````bash` — Markdown code fence boundary for a code block.
- L37: `g++ main.cpp cpu_scheduling/*.cpp memory_management/contiguous/*.cpp memory_management/non_contiguous/*.cpp page_replacement/*.cpp disk_scheduling/*.cpp utils/*.cpp -o MiniOS` — Markdown paragraph text for documentation.
- L38: ````` — Markdown code fence boundary for a code block.
- L39: `` — Blank line for readability and separation.
- L40: `## Execution` — Markdown heading that organizes the document.
- L41: `` — Blank line for readability and separation.
- L42: ````bash` — Markdown code fence boundary for a code block.
- L43: `./MiniOS` — Markdown paragraph text for documentation.
- L44: ````` — Markdown code fence boundary for a code block.
- L45: `` — Blank line for readability and separation.
- L46: `## Sample Output (Excerpt)` — Markdown heading that organizes the document.
- L47: `` — Blank line for readability and separation.
- L48: ````` — Markdown code fence boundary for a code block.
- L49: `===============================` — Markdown paragraph text for documentation.
- L50: `   Mini Operating System Simulator` — Markdown paragraph text for documentation.
- L51: `===============================` — Markdown paragraph text for documentation.
- L52: `Major Modules` — Markdown paragraph text for documentation.
- L53: `1. CPU Scheduling` — Markdown paragraph text for documentation.
- L54: `2. Memory Management` — Markdown paragraph text for documentation.
- L55: `3. Page Replacement` — Markdown paragraph text for documentation.
- L56: `4. Disk Scheduling` — Markdown paragraph text for documentation.
- L57: `5. Exit` — Markdown paragraph text for documentation.
- L58: `Select option: 1` — Markdown paragraph text for documentation.
- L59: `` — Blank line for readability and separation.
- L60: `[ FCFS Scheduling ]` — Markdown paragraph text for documentation.
- L61: `Enter number of processes (1-10): 3` — Markdown paragraph text for documentation.
- L62: `Arrival time P1 (0-50): 0` — Markdown paragraph text for documentation.
- L63: `Burst time P1 (1-50): 5` — Markdown paragraph text for documentation.
- L64: `Arrival time P2 (0-50): 2` — Markdown paragraph text for documentation.
- L65: `Burst time P2 (1-50): 3` — Markdown paragraph text for documentation.
- L66: `Arrival time P3 (0-50): 4` — Markdown paragraph text for documentation.
- L67: `Burst time P3 (1-50): 2` — Markdown paragraph text for documentation.
- L68: `Step: P1 runs from 0 to 5` — Markdown paragraph text for documentation.
- L69: `Step: P2 runs from 5 to 8` — Markdown paragraph text for documentation.
- L70: `Step: P3 runs from 8 to 10` — Markdown paragraph text for documentation.
- L71: `` — Blank line for readability and separation.
- L72: `PID   ARR       BURST     PRIO      COMP       WAIT       TAT        RESP` — Markdown paragraph text for documentation.
- L73: `------------------------------------------------------------------------------------` — Markdown list item that enumerates content.
- L74: `1     0         5         0         5          0          5          0` — Markdown paragraph text for documentation.
- L75: `2     2         3         0         8          3          6          3` — Markdown paragraph text for documentation.
- L76: `3     4         2         0         10         4          6          4` — Markdown paragraph text for documentation.
- L77: `` — Blank line for readability and separation.
- L78: `Averages: Waiting=2.33, Turnaround=5.67, Response=2.33` — Markdown paragraph text for documentation.
- L79: ````` — Markdown code fence boundary for a code block.
- L80: `` — Blank line for readability and separation.
- L81: `## Notes` — Markdown heading that organizes the document.
- L82: `- ANSI colors are used for a richer console UI. If colors do not appear, enable ANSI in your terminal.` — Markdown list item that enumerates content.
- L83: `- All modules print step-by-step simulation output and computed metrics.` — Markdown list item that enumerates content.

## File: utils\input_helper.cpp
- L1: `#include <algorithm>` — Include required standard or project header so symbols compile.
- L2: `#include <cctype>` — Include required standard or project header so symbols compile.
- L3: `#include <iostream>` — Include required standard or project header so symbols compile.
- L4: `#include <limits>` — Include required standard or project header so symbols compile.
- L5: `#include <sstream>` — Include required standard or project header so symbols compile.
- L6: `#include <string>` — Include required standard or project header so symbols compile.
- L7: `#include <vector>` — Include required standard or project header so symbols compile.
- L8: `` — Blank line for readability and separation.
- L9: `namespace ui {` — Begin namespace scope for UI helpers.
- L10: `` — Blank line for readability and separation.
- L11: `// Wrap ANSI color code in escape sequence.` — Comment added to explain intent or algorithm detail.
- L12: `static std::string ansi(const std::string& code) {` — Declare a file-local helper function.
- L13: `    return "\x1b[" + code + "m";` — Return from the function.
- L14: `}` — Close the current scope block.
- L15: `` — Blank line for readability and separation.
- L16: `std::string color(const std::string& text, const std::string& code) {` — Execute this statement as part of the algorithm or UI flow.
- L17: `    return ansi(code) + text + ansi("0");` — Create ANSI escape sequence for colored output.
- L18: `}` — Close the current scope block.
- L19: `` — Blank line for readability and separation.
- L20: `// Clear screen and move cursor to top-left for a clean UI.` — Comment added to explain intent or algorithm detail.
- L21: `void clearScreen() {` — Define a function that performs actions without returning a value.
- L22: `    std::cout << "\x1b[2J\x1b[H";` — Print formatted output to the console.
- L23: `}` — Close the current scope block.
- L24: `` — Blank line for readability and separation.
- L25: `void printTitle(const std::string& title) {` — Define a function that performs actions without returning a value.
- L26: `    clearScreen();` — Clear console for menu-like UI.
- L27: `    std::string line(title.size() + 8, '=');` — Execute this statement as part of the algorithm or UI flow.
- L28: `    std::cout << color(line + "\n", "36");` — Print formatted output to the console.
- L29: `    std::cout << color("   " + title + "\n", "1;36");` — Print formatted output to the console.
- L30: `    std::cout << color(line + "\n", "36");` — Print formatted output to the console.
- L31: `}` — Close the current scope block.
- L32: `` — Blank line for readability and separation.
- L33: `void printSection(const std::string& title) {` — Define a function that performs actions without returning a value.
- L34: `    std::cout << "\n" << color("[ " + title + " ]", "33") << "\n";` — Print formatted output to the console.
- L35: `}` — Close the current scope block.
- L36: `` — Blank line for readability and separation.
- L37: `// Read a validated integer within [minValue, maxValue].` — Comment added to explain intent or algorithm detail.
- L38: `int readInt(const std::string& prompt, int minValue, int maxValue) {` — Validated integer input routine.
- L39: `    while (true) {` — Loop while a condition remains true.
- L40: `        std::cout << prompt;` — Print formatted output to the console.
- L41: `        int value;` — Execute this statement as part of the algorithm or UI flow.
- L42: `        if (std::cin >> value) {` — Conditional check to choose a branch.
- L43: `            if (value >= minValue && value <= maxValue) {` — Conditional check to choose a branch.
- L44: `                return value;` — Return from the function.
- L45: `            }` — Close the current scope block.
- L46: `            std::cout << color("Value out of range.\n", "31");` — Print formatted output to the console.
- L47: `        } else {` — End a scope block.
- L48: `            std::cout << color("Invalid input.\n", "31");` — Print formatted output to the console.
- L49: `            std::cin.clear();` — Read input from the console.
- L50: `        }` — Close the current scope block.
- L51: `        // Discard the rest of the line to avoid infinite loops on bad input.` — Comment added to explain intent or algorithm detail.
- L52: `        std::cin.ignore(std::numeric_limits<std::streamsize>::max(), '\n');` — Read input from the console.
- L53: `    }` — Close the current scope block.
- L54: `}` — Close the current scope block.
- L55: `` — Blank line for readability and separation.
- L56: `std::vector<int> readIntList(const std::string& prompt, int count, int minValue, int maxValue) {` — Validated integer input routine.
- L57: `    std::vector<int> values;` — Execute this statement as part of the algorithm or UI flow.
- L58: `    values.reserve(count);` — Execute this statement as part of the algorithm or UI flow.
- L59: `    while (static_cast<int>(values.size()) < count) {` — Loop while a condition remains true.
- L60: `        std::cout << prompt;` — Print formatted output to the console.
- L61: `        std::string line;` — Execute this statement as part of the algorithm or UI flow.
- L62: `        std::cin.ignore(std::numeric_limits<std::streamsize>::max(), '\n');` — Read input from the console.
- L63: `        std::getline(std::cin, line);` — Read input from the console.
- L64: `        std::stringstream ss(line);` — Execute this statement as part of the algorithm or UI flow.
- L65: `        int x;` — Execute this statement as part of the algorithm or UI flow.
- L66: `        while (ss >> x) {` — Loop while a condition remains true.
- L67: `            if (x < minValue || x > maxValue) {` — Conditional check to choose a branch.
- L68: `                values.clear();` — Execute this statement as part of the algorithm or UI flow.
- L69: `                break;` — Execute this statement as part of the algorithm or UI flow.
- L70: `            }` — Close the current scope block.
- L71: `            values.push_back(x);` — Execute this statement as part of the algorithm or UI flow.
- L72: `        }` — Close the current scope block.
- L73: `        if (static_cast<int>(values.size()) != count) {` — Conditional check to choose a branch.
- L74: `            values.clear();` — Execute this statement as part of the algorithm or UI flow.
- L75: `            std::cout << color("Please enter exactly " + std::to_string(count) + " valid values.\n", "31");` — Print formatted output to the console.
- L76: `        }` — Close the current scope block.
- L77: `    }` — Close the current scope block.
- L78: `    return values;` — Return from the function.
- L79: `}` — Close the current scope block.
- L80: `` — Blank line for readability and separation.
- L81: `void waitForEnter() {` — Define a function that performs actions without returning a value.
- L82: `    std::cout << color("\nPress Enter to continue...", "90");` — Print formatted output to the console.
- L83: `    std::cin.ignore(std::numeric_limits<std::streamsize>::max(), '\n');` — Read input from the console.
- L84: `    std::cin.get();` — Read input from the console.
- L85: `}` — Close the current scope block.
- L86: `` — Blank line for readability and separation.
- L87: `void printSeparator(int width) {` — Define a function that performs actions without returning a value.
- L88: `    std::cout << std::string(width, '-') << "\n";` — Print formatted output to the console.
- L89: `}` — Close the current scope block.
- L90: `` — Blank line for readability and separation.
- L91: `} // namespace ui` — End a scope block.


